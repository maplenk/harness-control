# Codex diff-review ROUND 3 (VALID) of F7 fixes (commit 5994b49) — 2026-07-22

Round-2 fixes CONFIRMED good (workspaces fail-closed, ls-tree absence, fail-closed boundary). New findings are deeper edge/failure-path robustness. Verdict: NEEDS-FIX.

## 1. Prioritized findings

Reviewed on-disk HEAD `5994b49cbc68f80b6057128d584c0ae7d385f0ce`.

- **Blocker — completed-round resume can commit excluded `node_modules` or other unadjudicated dirt into HEAD.** A matching implementor commit is persisted `completed` despite provisioning failure ([deliverable.ts:21](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/deliverable.ts:21)); resume therefore skips implementation ([orchestrate.ts:253](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/orchestrate.ts:253)). But adoption treats every implementor round as interrupted and calls `validate()` ([orchestrate.ts:401](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/orchestrate.ts:401)), whose dirty-tree recovery performs unrestricted `git add -A` ([validate.ts:147](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/validate.ts:147), [validate.ts:195](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/validate.ts:195)). Concrete case: the implementor removes the ignore rule while a provisioned tree exists; the normal commit excludes `node_modules`, provisioning fails, then resume WIP-commits the entire tree. This corrupts the worktree branch and makes recovery fail permanently as “tracked node_modules”; other dirt can become the verifier’s new, never-adjudicated HEAD. Persist the host-verified implementation commit and make completed-round resume assert/reset to exactly it; all active-provisioning WIP staging must also exclude `node_modules`.

- **Blocker — failed swap rollback can destroy the only backup.** `swapIntoPlace` moves the old tree aside, but if both move-in and rollback fail, the backup remains in `stageDir` ([provision.ts:674](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:674), [provision.ts:680](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:680)). The unconditional `finally` then deletes that stage and sole backup ([provision.ts:578](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:578)). Preserve the stage whenever rollback is unconfirmed and throw `provisioning_failed`.

- **Blocker — filesystem errors are misclassified as absence, allowing stale `node_modules` to green a no-dependency round.** `lstatSafe` suppresses every error, not only `ENOENT` ([provision.ts:798](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:798)). Both safety preflight and the no-deps removal use that result ([provision.ts:382](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:382), [provision.ts:480](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:480)). A transient `EIO` can therefore become “absent”, return success, and leave a stale `.bin` for verification. Only `ENOENT` should mean absent; all other errors must fail closed.

- **Blocker — committed `.npmrc` can escape transactional staging and receives the full orchestrator environment.** `.npmrc` is trusted as an input ([provision.ts:269](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:269)), copied into the install stage ([provision.ts:649](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:649)), and `npm ci` receives `{...process.env}` with no forced stage-local cache/userconfig ([provision.ts:170](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:170)). A committed `.npmrc` can redirect npm cache/log writes into the primary checkout or expand ambient tokens into registry authentication. Provisioning happens before the primary-mutation snapshot ([implementor.ts:984](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/implementor.ts:984)), so such mutation becomes the baseline and is undetected. Use a minimal environment, isolated HOME/userconfig/cache, and reject or override path-escaping npm settings.

- **High — stage GC can still destroy backups outside the protected rename-failure branch.** When the worktree tree is missing, failure to enumerate a matching stage is silently skipped ([provision.ts:729](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:729)); the later sweep may then delete it ([provision.ts:761](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:761)). A transient read failure followed by successful removal loses the sole `old-*` backup. Additionally, prefix matching means GC for `asg-x` also matches `asg-x-y-*` ([provision.ts:721](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:721)). Unreadable stages must be preserved/fail closed, and stages need an exact, collision-free per-assignment namespace.

- **High — the standalone entry point permits active provisioning with normal `git add -A`.** `runImplementor` lets `options.provisionActive=false` override an active manager while still installing the manager’s default provisioning callback ([implementor.ts:1195](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/implementor.ts:1195)). That combination stages `node_modules` through `addAll` ([implementor.ts:966](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/implementor.ts:966)). The main loop threads this correctly, but the public standalone path does not. Derive the flag from the manager strategy or reject inconsistent overrides.

- **Medium — verifier-boundary provisioning errors are emitted without redaction.** `toProvisioningFailure` stores raw error detail ([orchestrate.ts:918](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/orchestrate.ts:918)), which the CLI prints ([commands.ts:1139](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/cli/commands.ts:1139)). The implementor-boundary path correctly uses `redactText`. Apply the same redaction here.

The workspace-key check itself is correctly presence-based ([provision.ts:228](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/provision.ts:228)); structural `ls-tree` absence handling is locale-independent and fail-closed on nonzero exit ([git.ts:358](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/worktree/git.ts:358)); and the loop’s normal/resume/failover verifier boundary is unconditionally reprovisioned before dispatch ([orchestrate.ts:712](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/orchestrate.ts:712)).

## 2. Verdict

**NEEDS-FIX.**

At minimum: preserve swap/GC backups on every uncertain recovery path; distinguish `ENOENT` from filesystem failure; bind completed-round resume to the exact persisted implementation commit; exclude `node_modules` from every active-provisioning reconciliation commit; isolate npm’s environment/filesystem configuration; and remove inconsistent `provisionActive` overrides.

## 3. Genuinely missing tests

Yes.

The existing adjudicator tests plus the `no_deliverable → resume re-drives implementor` integration test are sound for the narrow stage-routing rule ([implementor.test.ts:1075](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/implementor.test.ts:1075), [vertical-slice.test.ts:1218](/Users/tagtaste/Documents/QBApps/harness-orchestration/.claude/worktrees/agent-ad6b0180db834588b/src/app/flows/vertical-slice.test.ts:1218)). They are not sufficient M9 coverage because they never combine a real provisioning failure, on-disk excluded dirt, persistence, and resume. That missing test would expose the `validate()` WIP-commit bug above.

Also genuinely missing: double rename failure preserving the swap backup; non-`ENOENT` `lstat` failure on the no-deps path; unreadable/prefix-colliding stage GC; and hostile `.npmrc` confinement.
