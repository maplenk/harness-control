# Orchestrator reasoning notes — companion to PLAN.md Rev 2

Purpose: the full thought-trail behind every Rev 2 decision, including alternatives rejected, confidence levels, and known weak points. Reviewers: attack this reasoning, not just the plan text. Maintained alongside every plan revision.

## 0. Process that produced Rev 2
Local recon → Codex drafted Rev 1 independently → I verified Rev 1's riskiest claims against installed packages (ACP SDK grep) → 3 research passes (harness landscape; protocol landscape + prior art + memory; Intent-by-Augment local teardown incl. extracted asar) → user locked 3 scope decisions (limit-pause in MVP; memory = process supervision AND context economics; design package = visuals only) → Rev 2 consolidation → this review round.

## 1. Evidence log (what I actually verified vs. took on report)
VERIFIED MYSELF (local, high confidence):
- ACP packages exist at pinned versions; npm install succeeded (node_modules present).
- SDK 1.2.1 dist contains `session/set_config_option`, `session/fork`, `session/resume`, `session/list`, `session/close`, `session/set_mode`, `session/load`; zero hits for `session/set_model`.
- claude-agent-acp dist references `/login`, `.claude` config dir, CLAUDE_CODE_EXECUTABLE; depends on `@anthropic-ai/claude-agent-sdk@0.3.207`. So OAuth mechanics exist in the adapter regardless of ToS posture.
- codex-acp ships only dist/index.js in npm files + bun-compile scripts → platform binary acquisition path UNKNOWN (flagged §17; not yet traced).
- Repo state: git initialized by Codex, origin=github.com/maplenk/harness-control, 1 commit.
SCOUT-REPORTED (medium-high confidence, live-web-verified by them, not re-verified by me):
- All §3.2 harness facts; adapter issue numbers (#886/#883/#873/#864/#625/#582/#227); ToS timeline (2026-01-09 blocks, 02-19 ToS, opencode removal 03-19); Grok incident; ACP governance/JetBrains; A2A zero-harness adoption; AGENTS.md/AAIF governance.
SCOUT-REPORTED (single-source or inference, lower confidence — attack here):
- "#886 = 10-20s flaky model switch" is an issue-tracker anecdote, not a benchmark; could be fixed in a point release or worse in practice.
- Codex SDK spawn-per-turn/no-permission-callback: verified from SDK source by scout (strong), but version-pinned to 0.144.x — may change.
- Intent teardown claims: read from extracted asar by scout; 4 giant files only grepped (acp-provider.js 9397 lines etc.). The `session/set_model` usage in Intent = evidence it existed in an older protocol rev; I did NOT confirm in ACP spec history.
- Gemini headless resume "likely but unconfirmed".
CONTRADICTION RESOLVED: protocol-scout Part 3 said "no competitor uses ACP" (public info) — augment-scout's asar teardown proves Intent DOES (private implementation). Lesson applied: public research under-observes closed-source internals; where it mattered, we had local evidence.

## 2. Decision reasoning + alternatives rejected

### D-transport (PLAN §3.1/§3.2, debate a+b)
Chose: ACP-uniform MVP (both official adapters), SPI transport-agnostic, Claude-SDK adapter fast-follow.
Reasoning: one conformance suite, one failure model, one permission-mediation path for MVP; Intent precedent de-risks the bet; Codex side has no better option (native SDK objectively weaker — spawn-per-turn, no permission callback); Claude native SDK IS objectively stronger, but adding a second transport in MVP doubles the P2 test matrix for a capability (token-granularity streaming, in-process canUseTool) the MVP flow doesn't strictly need.
Rejected alternatives: (i) best-surface-per-harness now (Claude SDK + Codex ACP) — better ceilings, more MVP surface area; (ii) headless-JSON everywhere (claude -p --input-format stream-json is genuinely capable and bidirectional) — no permission RPC on Codex side, hand-rolled session semantics; (iii) PTY — strictly dominated, all 8 harnesses have structured headless.
Weak point to attack: if #886-class adapter flakiness is systemic rather than transient, ACP-uniform could make the flagship switch flow unreliable at MVP; the fallback is checkpoint-successor which always works but loses native continuity.

### D-flagship detection (PLAN §13)
Chose: per-adapter classifier + 3-tier confidence + forecast engine.
Reasoning: ACP genuinely has nothing (closed StopReason enum verified); Claude adapter convention (errorKind) exists since Apr 2026; Codex structured snapshot never crosses (#227). The 3-tier vocabulary conveniently matches the user's design language (honored/forecast_fallback/forecast_only) — but note direction of inference: protocol reality independently produced the same tiers the user's mock assumed; I did NOT design from the mock.
Weak points: (1) live error-string characterization is an open research gap (harness-scout timed out before the addendum) — classifier fixtures start from recorded transcripts we don't yet have; (2) forecast engine is the most speculative component in the plan — per-provider window models fitted from observed resets could be wrong by hours; mitigation is honesty (source label + confidence) not accuracy; (3) probe-at-ETA burns a turn — on API-key billing that's cents, on limited plans it may re-trip the limit; jitter/backoff parameters are guesses.
Judgment call to review: I imported limit-pause MECHANICS (checkpoint-first, no-respawn, resume paths) from the user's design package despite "visuals only" — justified by the user separately confirming the feature in MVP. If reviewers think any imported mechanic is UI-fantasy rather than sound engineering, say so.

### D-supervision (PLAN §14)
Chose: pgid-based registry, RSS tree sampling, 75/100% thresholds, bounded restarts→breaker, RAM-scaled caps (Augment's exact tiers), LRU idle eviction.
Reasoning: Augment's numbers are production-tested defaults from a shipped product; copying beats inventing. Checkpoint-then-terminate at 100% (vs kill -9) preserves work; restarts counting toward breaker unifies OOM-kill and crash handling in one loop.
Weak points: (1) checkpoint-at-100%-RSS may be impossible if the child is thrashing — may need a hard ceiling above which we kill first and reconstruct from last periodic checkpoint (periodic checkpoint cadence is NOT yet specified in the plan — real gap); (2) watchdog termination during a child's git write can corrupt the worktree — worktree mutex covers add/remove, not arbitrary writes; termination should prefer SIGTERM+grace before SIGKILL and the verifier re-validates worktree cleanliness (partially specified, could be tighter); (3) LRU eviction of idle ACP processes assumes cheap resume — true only where session/load-or-resume is solid (Claude adapter yes; Codex resume = new process re-read, cost unknown).

### D-memory (PLAN §15)
Chose: ADD-only entries + bi-temporal invalidation + scope tiers + shared exploration artifact + per-role budgets + summarizing compactor (background job).
Reasoning: ADD-only (mem0) eliminates concurrent-writer conflicts (multiple agents write memory simultaneously); bi-temporal (Zep) fits spec supersession (old constraints stay queryable); summarizing compactor is the direct differentiator vs Intent's truncation — their weakness observed in code, not marketing.
Weak points: (1) risk of overbuilding — MVP has ONE implementor; concurrent-writer machinery is future-proofing that could be YAGNI (my defense: schema-level choices are cheap now, migrations later are not); (2) summarization-in-checkpoints introduces an LLM call into the pause path — pause must not BLOCK on it (write verbatim core synchronously, summarize async — this ordering is implied but not explicit in the plan; tighten if reviewers agree); (3) per-role budgets need numbers — I have none evidence-based yet.

### D-states (PLAN §6.1)
paused_limit as sibling-of-active (re-entrant from implementing/verifying/specifying) vs a substate: chose sibling for event-log clarity and UI mapping; cost is transition-matrix growth (limit during awaiting_approval? during model-switch confirm? — matrix not exhaustively enumerated in Rev 2, known gap, reviewers please enumerate).
breaker_open only from implementing/verifying — is that right? A coordinator (read-only) crash-looping should probably also breaker. Not settled.

### D-WS (PLAN §1.4/§18)
Position logged pre-research and survived it: stdio for control (process lifecycle = connection), WS as event-bus subscriber replaying from the SQLite sequence. AG-UI evaluated: right layer, but single-run abstraction lacks multi-agent hierarchy — custom event schema now, AG-UI mapping possible later. serve-in-MVP left to user (D4) because it's pure scope, not architecture.

### D-scope/ceremony (D7)
The usefulness risk is real: full flow = spec → human approval → implement → verify → merge-readiness = heavy for small tasks. --quick lowers to auto-approved micro-spec + combined implement/verify. Invariant question for reviewers: does auto-approval below a threshold break the "human owns the spec" principle, or is threshold-gated auto-approval (size/risk heuristic + always-overridable) an acceptable relaxation? My lean: acceptable and necessary; a tool nobody uses for small tasks loses the daily-driver position.

## 3. Standing uncertainties (ranked)
1. Live limit-error formats per harness/version (blocking P4 fixtures; plan says characterize at build time).
2. codex-acp platform-binary provenance (supply chain; doctor check specified, mechanism untraced).
3. node:sqlite maturity on Node 22.14 (D6; 5-minute check at P1 start).
4. Adapter flakiness magnitude (#886) — anecdote vs systemic.
5. Forecast engine viability without structured reset data (Codex path especially).
6. Whether ACP required/optional session-method boundary shifts under us (CHANGELOG 1.3.0 signal; capability-probe is the hedge).
7. Billing: whether Codex subscription headless is usable at all (docs favor API key; unconfirmed).

## 4. What I deliberately did NOT include
(superseded in part by Rev 3 — see §5)

## 5. Review reconciliation → Rev 3 (2026-07-18)
Reviews: Codex gpt-5.6-sol xhigh (`docs/reviews/codex-sol-review.md`, conditional no-go, 7 conditions) + Opus thinker (transcript; 3 conditions, 2 cuts). Convergent on ~90%; all convergent findings ACCEPTED.

ACCEPTED (both reviewers) → Rev 3 changes:
- State machine rebuilt as phase × suspension × operation with normative transition table T1-T25 + generated conformance tests (§6.2/6.3). Sol #1 + thinker F3/N9. Suspension attaches to assignment/segment; run status is a projection — resolves the "limit while awaiting_approval" class (T7/T8).
- Checkpoint contract: synchronous mechanical core, artifact-then-event atomicity, periodic cadence (3 turns), `incomplete_operation` honesty; "covers all work" language removed; LLM narrative summaries deferred entirely. Sol #3 + thinker N8/F2/F12.
- Process identity (pid+start-time+exe+nonce) before any signal; watchdog participates in git-op leases; TAINTED worktree state + validation (index.lock cleanup, reconcile-or-refuse) before lease release/restart/verify. Sol #2 + thinker F11/F-git.
- Fail-safe classification: envelopes only (agent free text never classifies — Sol's false-positive scenario), unknown_provider_error pauses and never feeds breaker (thinker F1), parsed tier behind a reviewed real-transcript corpus gate (both), errorKind conformance fixture pinned per adapter version (thinker F9).
- Breaker: window + non-disableable lifetime cap + no-progress hash detection + max recovery time; `off` → `--unsafe-dev`, lifetime cap retained. Sol #4 + thinker F8.
- CUT from MVP: learned forecast engine (honest `unknown` ETA + bounded probe ladder instead) — Sol cut list + thinker F6/N10/Cut2; RAM-tier scheduler + LRU eviction (simple max-children=3) — Sol #13 + thinker F7/F4/Cut1; `serve` (D4), MCP injection (D5), D7 `--quick` as specified (invariants unwaivable; `--fast` redesigned + deferred), bi-temporal/global memory + LLM compactor (Sol g/#9; ADD-only-vs-invalidated_at contradiction conceded — if ever added, invalidation = append-only edge).
- Verifier independence: exploration artifact = untrusted index bound to source commit, never evidence. Sol #10.
- Cost: soft estimated budget + reservation + refusal policy + per-role attribution surface. Sol #11 + thinker's under-investment call.
- Quotas/retention for events/artifacts/queues; orchestrator self-supervision + heartbeat. Sol #8 + thinker F10.
- SPI `resumeSession?` with explicit semantics + conformance tests. Sol #6.
- Self-containment: all Rev 1 normative content (transport limits, tests) inlined; test list renumbered 1-33. Sol #14.
- §3 evidence language: "proves" → "proves feasibility, not portability/reliability/convergence"; precise ACPProvider instantiation citation (re-verified locally, provider-registry.js:6-58,107 — evidence UPGRADED from grep-strings to instantiation path; thinker's N1 deletion claim was factually wrong, scratchpad intact, but its epistemic point stood and is honored). Non-ACP fallback seam restored as executable contract (Sol b + Rev 1 defense).
- P4 split into 4a/4b/4c with evidence gates. Sol scope section + thinker Condition 3.
- P2 live compatibility gate; systemic Claude-adapter failure promotes SDK into MVP. Sol (a)/D1 + thinker D1 (its "spike SDK early" folded into the gate).

DISAGREEMENTS RESOLVED (my calls):
- D6 driver: Sol said spike-and-select (no textual basis to preselect); thinker said better-sqlite3. I verified locally: node:sqlite emits ExperimentalWarning on 22.14.0 → better-sqlite3 DEFAULT, contract tests dual-run (evidence Sol lacked; both intents honored).
- Coordinator default (D3): thinker leaned Claude-for-structured-signal; Sol said no hard-coded default without comparative evidence. Took Sol's ruling (config-required), kept thinker's insight as a docs note.
- Thinker's risk-tiered ceremony: direction accepted, but deferred with `--fast` (Sol showed any auto-approval breaks invariants; spec-revision verb + spec-proposed default profiles shipped in MVP instead — the cheap 80% of the ceremony fix).
- Sol's "no evidence for Codex subscription claims": accepted — 3-state auth reporting in doctor; no categorical wording.

REJECTED (with reason):
- Thinker F4's "proactively close paused processes, always successor-resume": partially — MVP has no LRU (cut), and §11.1 already prefers native resume with successor fallback; forcing successor-always would discard the Claude adapter's working session/load-resume for no gain. Kept resume-when-identity-confirmed.
- Sol's implicit preference to drop `paused_user`… not requested; T11/T12 added instead (CLI `pause` needed a defined state — Sol #1 flagged exactly this gap).

## 5b. Build status (live)
- P1 DONE (verified, 370 tests). P2 DONE (verified unconditional 2026-07-18, 791 tests). P3 DONE (verified unconditional 2026-07-18, 900 tests) — the cross-harness loop runs through the SHIPPED CLI with zero shims: Claude Opus(low) coordinator drafted a valid spec, T1 approval, Codex gpt-5.6-terra implementor created greeting.txt (real commit c254435, ChatGPT-sub via H-1 isolation), Claude Opus(low) verifier confirmed per-criterion, reached merge_ready with §16 manual integration commands (correct 'master' branch, never executed). Real spend $0.55 measured + $0.50 estimated (subscription). Evidence: docs/reviews/p3-live-acceptance.md.
- P3 residual (all low, non-blocking): F-1 `run` requires explicit --implementor/--verifier (spec-proposed profile names not yet resolvable as defaults); F-2 --test-approve without --spec-hash fabricates a synthetic hash (test seam only, not loop-correctness); D-2 Codex subscription cost is an estimated reservation not measured (no per-token price crosses). Next: P4a/b/c (flagship pause/resume, failover+switching, context economics) behind their evidence gates. Live gate: BOTH adapters pass verbatim incl. real permission-deny (Claude Run 2; Codex Run 3 after H-1 isolation — deny fired, file not created, hostConfig warning fired, auth honestly detected_but_unvalidated).
- P2 residual watch-list (non-blocking, carry into later phases): (a) copy-in Codex auth doesn't propagate refresh back to ~/.codex — re-copied per run; single-use-refresh rotation would surface as an auth-classified failure, not home mutation. (b) per-run isolated CODEX_HOME means codex-native cross-spawn session history doesn't persist → §11.1 resume relies on checkpoint-successor for Codex, native within-run only (fine — matches the plan's portable path). (c) `CODEX_HOME` is now isolation-owned and caller/spawn-override replacement is rejected; non-protected env remains a dev/test seam. (d) invalid host OPENAI_API_KEY still forwarded per D2 but now reported honestly; working Codex path is inherited ChatGPT login. (e) production Claude now uses the installed native provider with `--safe-mode`, empty strict MCP config, and role-scoped tools; its real `dontAsk` Bash behavior and rate-limit envelope are gated by `npm run smoke:claude:provider`.

## 5c. External review round (2026-07-18, user-supplied verdict) — verified 8/8, drives Wave 1 + P4a
An external review (delivered by the user mid-P4 planning) reported 4 critical + 4 high findings + release hygiene. I verified EVERY citation against the code before acting — all 8 reproduce exactly as cited (verifier.ts:856 unconditional T24; service.ts:508/611 no supervision/classification, childActive never set; commands.ts:250/304 approval-hash unbound; implementor.ts:442→463 commit-before-verify + probe checks destination only; index.ts:87/103/127 config/quota not durable + cost.ts:244 estimated spend uncounted; service.ts:415 projection-without-event; commands.ts:110 revise dead-end; model-resolution.ts:190 + service.ts:529 silent pin failures). Response plan: Wave 1 workflow fixes 1/3/4/5/6/7/8 + hygiene (all mechanically verified, low design risk); finding 2 IS P4a's mandate → folded into the P4a build (Wave 2). Spec for both: docs/specs/hardening-p4a.md. Codex (gpt-5.6-sol xhigh) gets the P4a design section for pushback in parallel with Wave 1; Codex reviews the full diff after Wave 2 (user instruction: "use workflows and codex as reviewer").
Key design calls needing scrutiny (sent to Codex): (a) merge_ready now asserts criteria AND MergeReadiness.ready; git-blocked readiness → T23 with integration_blocker fix requests (normative T23 amendment; probe-absent = blocked, fail-safe); (b) resume granularity = role-round restart from checkpoint, honest about lost in-context work (§12.3 distinction), NOT mid-turn token resume; (c) probe = cheapest no-op turn on throwaway session, deterministic jitter from (runId, probeIndex) hash, plan recomputed from event log (no in-memory timers); (d) initial-spawn pin failure = hard typed error after one retry (NOT successor — that's for mid-run switches, P4b); (e) config binds at start, persisted per-run projection; (f) crash auto-respawn deferred to P4b (T13 counters fold now; manual resume via checkpoint); (g) "untracked tree" review item deliberately NOT actioned — the user's standing rule is the orchestrator never commits.

## 5d. Wave 1 hardening — DONE (2026-07-18, workflow wf_c7f9d415-e1a, 7 agents, all verdicts fixed)
All 7 findings + hygiene fixed and adversarially verified: 931 tests green (901→931), typecheck/build/pack clean, bin executable with shebang, pack = dist+profiles+README only. Highlights: merge_ready now provably requires MergeReadiness.ready===true (only T24 producer is gated on readinessBlockers.length===0; probe-absent = fail-safe blocked; PLAN §6.3 T23 + §16 amended); FixRequest became a discriminated union (criterion | integration_blocker); GitMergeFacts.worktreeClean REQUIRED so no fake probe can skip the W1-F4 gate; approval binds draft hash end-to-end (approve/run/test-approve — F-2 residual closed); RUN_CONFIG_PROJECTION makes config durable (binds at start; --config elsewhere = usage error); budget refusal counts measured+estimated+reservation (the mutation test reproduced the original unbounded-spend loop live); spec revise completes the full round (T2 → coordinator re-drive → revision N+1 supersedes → awaiting_approval); dispatch advances are replayable events with loud WorkflowDispatchReplayError on corrupt logs; model pins enforced (one retry → typed ModelPinError, session disposed). Verification quality: verifiers ran REAL mutation testing (revert-to-old-behavior, confirm exactly the new tests fail, restore SHA-verified byte-identical). New follow-ups logged in spec W2-0: public-ingest() guard for dispatch events; echo-mismatch pins treated as failures in W2-1/P4b. Nothing committed; tree unstaged for user review.

## 5e. Codex P4a design pushback (gpt-5.6-sol xhigh, 2026-07-18, 20m29s, read-only) — verdict "redesign", adopted into spec W2 Rev 2
11 items (9 blocking, 1 should-fix, 1 additional). Dispositions:
- ADOPTED 1 (readiness): T23-for-all-integration-blockers was wrong — user-actionable blockers burned remediation rounds toward a false `failed` terminal. Rev 2 splits by actionability: worktree-dirt stays T23 (agent-fixable, Wave-1 behavior kept); destination-dirty/base-drift/conflicts → durable `merge.readiness.blocked` + stay `verifying` + `recheck` CLI + no remediation increment; probe-absent upgraded to typed orchestration error; wrong-commit = typed error; T24 payload-validated (ready:true) closing the conformance-generator escape Codex spotted. NOTE: this reworks part of landed Wave-1 behavior — deliberate.
- ADOPTED 2 (resume adoption): RoleRoundProjection + role-specific reconciliation (implementor: WIP-commit; verifier: force back to implementationCommit, DISCARD dirt, same immutable binding); worktreeClean always re-probed.
- ADOPTED-WITH-DEVIATION 3 (probe): inconclusive-event for non-limit probe failures (no T10/breaker), event-anchored deadlines, cap renamed maxProbesPerIncident (permanent exhaustion documented; sliding window rejected as complexity). DEVIATION: probe stays a fresh throwaway session — under fresh-session-per-round architecture a probe pinned to the SAME model/effort is equivalent evidence to "the actual successor session"; retaining a live session through the runRole seam buys no proof. Codex's real defects (pin parity, non-limit path) are fixed.
- ADOPTED 4 (crash-safe wait): fenced probe claims (runId,incidentId,probeIndex) as idempotency keys; T9 no longer sets childActive — resume_reentry_pending projection + child.spawned + resume_reentry.completed ack; startup reclaims unacked re-entries; crash-injection tests at every boundary.
- ADOPTED 5 (pin stranding): pending/active dispatch split — phase advances ONLY after pins succeed; pin failure leaves a retryable pending round, nothing stranded.
- ADOPTED 6 (T13/generations): activeChild = {generationId, segmentId, status}; generation-matched child.stopped (kills the late-stop race); T13 amended for P4a → suspension=interrupted + counters, NO restart emission (real respawn = P4b); T11 completes only after stop confirmation.
- ADOPTED 7 (T4 licensing): new operation `initial_config_pin`; T4 accepts prompt_turn|initial_config_pin; T5 reserved for mid-run switches; classification precedes the W1-F8 retry (a limit envelope on a pin attempt pauses, never retries).
- ADOPTED 8 (Wave-1 conflicts): transactional resume-eligibility check (checkpoint.specHash == assignment.specHash == approvedSpecHash == draft.specHash, non-stale assignment) refusing WITHOUT clearing suspension; T10 reducer purified (scheduling out of transitions.ts hard-coded ladder → pure scheduler on per-run pinned config + explicit scheduled event).
- ADOPTED 9 (pause atomicity — sharpest catch): my checkpoint→stop→T4 ordering had a crash window (stopped child, no durable incident, orphaned artifact). Rev 2: pauseForLimit transaction — CAS fsync → ONE atomic append (T4/T16 + checkpoint.recorded + incident + stop-intent, generation `stopping`) → then dispose → generation-matched child.stopped; restart path defined for every crash point.
- ADOPTED 10 (supervision seams): ProcessIdentity on RoleAdapterHandle; registry on durable SQLite store persisted BEFORE child.spawned; startup reaps only persisted verified generations; nonce verification best-effort with §14 withhold-on-ambiguity.
- ADOPTED 11 (coordinator coverage): RoleRoundProjection generic across all three roles; ONE shared LimitPausedError handler for start/spec-revise/run; resume dispatches by pending round kind; test 24 gains coordinator + revision-coordinator variants.
Wave 2 workflow builds from spec W2 Rev 2. P4b draft (scratchpad) inherits: generation-tracked children, interrupted-resume as the respawn seam, probe design.

## 5f. Prior-art addendum (2026-07-19, user-prompted): OpenAI Swarm / Agents SDK
Checked github.com/openai/swarm on user request. Not in the original 13-competitor sweep because it sits one layer BELOW this product: Swarm composes agents from raw Chat Completions calls in-process (agents + handoffs, stateless, client-owned loop; educational-only, officially replaced by the OpenAI Agents SDK for production). Our bet is the opposite layer — orchestrate full coding HARNESSES via ACP to reuse their edit loops/permissions/sandboxing. Conceptual cross-checks: Swarm handoff ≈ in-process cousin of our checkpoint-successor (it can share raw history inside one process/API; we cannot across providers — which is exactly why the §12.2 mechanical checkpoint exists); its client-owns-all-state model independently validates our host-owned event-log design; routines ≈ role profiles. It has no durable state, pause/resume, supervision, or permission mediation — the hard parts here. No plan change. Watch item: OpenAI Agents SDK is the live successor if we ever need a raw-API agent tier (we deliberately do not for MVP).

## 5g. Wave 2 (P4a flagship) — BUILT + adversarially verified (2026-07-19, wf_14341d76-e0f, 12 agents; one transient API-connection failure mid-run, resumed from cache cleanly)
All 7 stages done: 1093/1093 tests green (931→1093), typecheck/build/pack clean, help documents resume/recheck/--no-wait. 17/20 verification claims HOLD outright — including: pauseForLimit is genuinely one transaction (both drivers, rollback-proven); T16 never feeds the breaker; scheduler pure + event-anchored + config-pinned; probe pinned to the resumed role's model; eligibility chain (checkpoint==assignment==approved==draft hash) blocks superseded-spec resurrection; verifier-dirt discarded on resume; coordinator + revision-coordinator pauses resume; readiness split works (blocked/recheck/T24, worktree-dirt still T23); supervision registry durable + identity-verified reaping; watchdog/heartbeat run and stop on every dispose path.
3 residuals (fix workflow launched, wave2-residuals): (R-A) commit-before-stop ordering correct in code but not test-enforced — dispose-before-append mutation passed all tests; (R-B) demonstrated narrow race: adopted claim + slow-alive waiter → deduped trigger re-folds projection → probeCount displays 2 while log correctly holds 1 (blast radius: status counter only; scheduler/cap event-derived); (R-C) SECURITY: limit.probe.inconclusive.detail embeds raw provider envelope text unredacted into the durable log + stdout + status --json (realistic vector: 401 messages echoing key fragments) — violates redaction-before-every-sink; fix = redactText in describeRawError.
Watch-list addition (pre-existing, NOT a P4a regression): transport #killGroup signals its own live child's process group without ps re-verification (transport.ts ~727-740) — the §14 signalVerified gate covers supervisor paths; assess promotion to full verification in P4b/P5.
Next after residuals: Codex full-diff code review (user-designated reviewer), then HTML refresh, then P4b.

## 5h. Residual round 1 (wf_d9a5f720-736) + redaction round 2 (wf_77f13d9b-877, in flight)
Round 1 outcome (1098 tests): R-A CLOSED (dispose-before-append mutation now fails exactly the 2 new ordering assertions; restored SHA-verified); R-B CLOSED (deduped trigger skips fold+save; new IngestResult status 'deduped'; probe path reports already_resolved; loud type-conflict path re-proven on both drivers); R-C PARTIAL — the requested scenario redacts, but the adversarial verifier FALSIFIED the absolute invariant two new ways: (1) raw-throw bypass — #routePinFailure/#routeProviderFailure throw the raw AdapterError which errorOutput/cli-index render unredacted into text+json (empirical: 5 synthetic fragments verbatim through real `harness start`); (2) ASSIGNMENT_RE shielding — value class doesn't exclude '=', so a non-sensitive `word:`/`word=` prefix greedily consumes a following `API_KEY=secret` (describeRawError's `${kind}: ` prefix makes this systematic); assignment-carried opaque secret reached the durable event + status + CLI. Round 2 fixes in flight: layered (sink redaction in errorOutput + cli/index handler AND source redaction on the typed throws) + structural pattern fix (sensitive-key alternation IN the regex so non-sensitive assignments never consume/shield; '=' allowed inside sensitive values) + honesty note (shapeless prose secrets are out of pattern scope by design). Verifier replays the exact falsification probes + hunts further error-shaped routes.

## 5i. Redaction rounds 3-4 (wf_3f861af4-bb1 done; wf_4a729006-305 in flight)
Round 3 (1122 tests): the unescaped-quoted-value gap is SOLID — 70k-case marker-oracle fuzz, zero leaks, zero idempotency failures, exact prior falsification byte-exact redacted end-to-end; round-2 corpus untouched. But the verifier's whole-message hunt found the ESCAPE CLASS beneath it, all via describeRawError's flat-string sink (redactDeep architecturally unreachable there): (L1) nested-JSON literal escapes hide a sensitive key behind \" (outer key non-sensitive); (L2) unterminated/truncated quote — no branch matches, full value verbatim; REALISTIC because our own .slice(0,200) at service.ts:2832 can cut a closing quote (slice-then-redact ordering bug); (L3) leading-escaped-quote leaks whole secret (round-3 fixture understated as tail-only). Round 4 = STRUCTURAL close-out: JSON-string-grammar quoted branches, escape-run delimiters (depth 0-3, parse+redactDeep secondary belt if regex insufficient), sensitive-key-gated unterminated-quote fallback, REDACT-BEFORE-TRUNCATE audit+enforcement, fixture severity fixes, precise no-overclaim docs. CONVERGENCE RULE set (in patterns.ts header): after round 4 the pattern layer is converged — further text-path findings are documented-accepted unless a whole SHAPED-or-KEYED secret leaks through a DURABLE sink. Redaction loop ends here; next gate = Codex full-diff review.

## 5j. External review 2 (2026-07-19, user-supplied) — mid-Wave-2 snapshot; triaged 2 stale / 5 live
Reviewer's tree was mid-Wave-2 (1,009 tests vs current 1,122): F1 (resume never resumes work) and F7 (no --wait/--no-wait, eta always unknown) are STALE — the Wave-2 S6 execution layer landed exactly this and was adversarially verified (orchestrate resolveResumeEntry/adoptWorktree; args --wait/--no-wait/recheck; commands resumesAt structured-when-present). LIVE and accepted: F2 [P0] verification-runner sandbox escape (host shell + FULL inherited env + cwd-only; probe proved credential visibility + out-of-worktree write — worst finding to date, invalidates the primary-checkout guarantee via npm-script editing); F3 [P1] cross-process pause/cancel never signal the live child (in-process limit path fine; fix = identity-verified signal through the durable registry); F4 [P1] live HALF: user-origin free text (goal/feedback/role inputs) persists unredacted (CLI-error half already fixed rounds 2-4); F5 [P1] draft saved AFTER awaiting_approval advance, projection-only → crash strands an approval-ready run unreplayably; F6 [P2] MaxLiveChildrenGuard unwired + git mutex process-local. Spec section W3 (W3-1..W3-5) written with MVP-honest fixes (env allowlist + primary-checkout mutation guard + documented no-OS-sandbox residual for W3-1). W3 wave queued behind redaction round 4 (file overlap). switch-model placeholder remains P4b by design.

## 5k. External review 3 (2026-07-19, user-supplied) — FRESH pre-W3 snapshot; 3 in-flight / 3 new-live / 1 user-owned
Snapshot dating: cites 66 files / 1,145 tests = EXACTLY the baseline verified minutes before the W3 launch — fresh, not stale. Security excluded per user request (covered by redaction rounds 2-4). Triaged per [[external-review-triage]]:
- F1 (cross-process pause/cancel), F2 (coordinator draft atomicity), F3 (concurrency no-op: guard unwired + process-local mutex) = IN-FLIGHT — independent re-confirmation of review-2 F3/F5/F6; W3-2/W3-4/W3-5 already building (wf_b937d673-ee0). No action beyond the running wave.
- F4 [P1] LIVE+NEW (config honesty): verified — failoverPolicy schema-accepted (schema.ts:264) with ZERO consumers; RestartBreaker (breaker.ts:159) has no non-test caller; CadencePolicy is a finished tested library production never invokes. → W4-1: wire cadence invocation at completed-turn boundary + wire breaker into the crash path (both pure wiring of done libraries), and REJECT non-'wait' failoverPolicy values until P4b implements them (reviewer's rec adopted: accepted config must act or be refused).
- F5 [P1] LIVE+NEW (switch-model): verified — commands.ts:1467 fabricates segmentId(`runId:pending`); in-code comment admits the engine rejects it; README:89 still advertises the command. → W4-2: route through W3-2's identity-verified registry channel (owner process performs set_config_option at a completed-turn boundary, confirms, appends T19). Design finalized ONLY after W3-2's channel shape lands; interim fallback if channel shape doesn't fit: gate the command with an honest no-live-session error + README caveat.
- F6 [P2] LIVE+NEW (telemetry): verified — aggregateWindow has zero production callers; status reads aggregates only → RSS null forever, raw samples grow unbounded. → W4-3: schedule window aggregation + prune, status falls back to latest raw sample when aggregates absent.
- Completeness notes: P4b/P4c absence = roadmap (PLAN.md phases), not defects. UNTRACKED SOURCE (only README.md committed) = deliberate consequence of the standing never-commit constraint; every change stays unstaged for user review. Resolution is the USER's: review + commit, or explicitly authorize staging/committing. Surfaced in reply; not actionable by orchestrator under current constraints.
- Sequencing: W4 spec written AFTER W3 completes (W4-2 depends on W3-2; W4-1/W4-3 touch service.ts/commands.ts which W3 stages are editing). Then W4 wave → Codex full-diff review (now covering W1+W2+redaction+W3+W4) → HTML refresh → P4b.

## 5l. External review 4 (2026-07-19, user-supplied) — FRESH current-tree snapshot; verified read-only mid-wave
Snapshot dating: cites 1,188 tests = EXACTLY the current tree (post W3-1/W3-3/W3-4, the tree the finish-W3 wave wf_2a60b260-038 is building against). Fresh, not stale. Security excluded per reviewer. Triaged read-only (the wave owns the write path — did NOT edit service.ts/commands.ts/PLAN/spec to avoid clobbering concurrent agent writes; note line numbers already shifted because W3-2/W3-5 code is landing). Verified every finding:
- F2 [P1] cross-process pause/cancel don't control live child = IN-FLIGHT (W3-2, building now). Extra detail to fold into the W3-2 verifier: user-pause emits checkpoint.requested but writes NO checkpoint; pause→resume can create TWO writers on one worktree (ties to the W3-5 lease).
- F5 [P1] git leases process-local = IN-FLIGHT (W3-5, building now; #admitSpawn already visible at service.ts ~2606). Reviewer wants SQLite/fs lease keyed by repo+assignment w/ generation/owner fencing — compatible with the W3-5 spec (file-based advisory lease under .git + §14 identity). Verifier must probe false-positive stale-reclaim (healthy pid) AND missed-reclaim (dead pid).
- F3 [P1] switch-model placeholder = QUEUED W4-2 (already triaged §5k). New sub-detail: implementor/verifier CURRENT models are not persisted either → W4-2 must persist effective model per role. Reviewer endorses gate-until-channel-exists (my fallback).
- F4 [P1] reliability config not enforced = SPLIT: MaxLiveChildrenGuard wiring is IN-FLIGHT (W3-5); failover-reject + RestartBreaker wiring + CadenceTracker wiring = QUEUED W4-1 (§5k).
- F7 [P2] telemetry never aggregated/pruned = QUEUED W4-3 (§5k).
- **F1 [P1] NEW-LIVE — restart recovery strands role runs.** VERIFIED: reapOrphanProcesses (service.ts) reconciles ONLY `activeChild.status==='stopping'` (line ~2585); a crash while a role generation is `active` gets SIGKILLed but emits NO interrupted transition, so suspension stays `none`. handleResume is coordinator-only for the unsuspended case (commands.ts ~1094, explicit comment) → active implementor/verifier crash AND the implementor-complete→verifier-not-dispatched boundary are unresumable. Fix (W4, needs thinker/Codex design pushback — core restart-safety, extends W3-4 atomicity into role phases): (1) startup reconciliation emits interrupted (T17/T13) for reaped `active` generations so resume can reclaim; (2) resume/dispatch drives interrupted implementor/verifier rounds via RoleRoundProjection; (3) persist a pending verifier round BEFORE implementor child disposal so the handoff gap is reclaimable. THIS IS THE MOST IMPORTANT NEW FINDING.
- **F6 [P1] NEW-LIVE — supervision.** (a) ps.ts is Darwin-only BY DESIGN (header says Linux flags differ, out of scope) but package.json/README are not platform-gated → MVP-honest fix: declare `os:["darwin"]` + README note, Linux impl = roadmap (patterns.ts-style honesty). (b) watchdog #scheduleNext does `void #tick().then(...)` with NO `.catch()` (watchdog.ts ~216) → a ps/execFileSync throw becomes an UNHANDLED REJECTION that can crash the process or silently kill supervision. Fix: `.catch()` → durable supervision-failure alert + reschedule (fail-open-with-alert) or fail-closed. Real robustness bug.
- **F8 [P2] NEW-LIVE — shell quoting.** manualIntegrationCommands (verifier.ts ~839) interpolates repoRoot/dest/ref unquoted into printed `git -C ... / switch / merge` commands. These are PRINTED-NOT-EXECUTED (no injection risk to the harness) but a path/ref with spaces yields invalid copy-paste. Fix: shell-quote or emit structured argv. Low-risk.
- Untracked-source point: same as §5k — deliberate consequence of the never-commit rule; user-owned resolution.
W4 scope after this review = W4-1 (config honesty: failover-reject + breaker + cadence), W4-2 (switch-model via W3-2 channel + per-role model persistence, or honest gate), W4-3 (telemetry aggregate/prune + raw fallback), W4-4 [NEW] restart-safety (F1 — reconcile active generations + role resume/dispatch reclaim), W4-5 [NEW] supervision hardening (F6: platform declaration + watchdog .catch()), W4-6 [NEW] shell-quote merge instructions (F8). W4-4 is the design-heavy one → thinker + Codex pushback before building. W4 waits for the finish-W3 wave to settle (shared service.ts/commands.ts; W4-2 needs W3-2's channel shape).

## 5m. Finish-W3 wave (wf_2a60b260-038) — DONE 8/8 agents; mechanical green 1205 tests / 72 files; 3 residuals → residual round (wf_cd99b7b8-ebf)
Landed + adversarially verified:
- W3-2 cross-process child control: BUILT + HOLDS (all 4 exploit probes defeated). CLI pause/cancel now await service.stopExternalChild ({escalate:false}/{escalate:true}); reads durable activeChild generation, declines when live in THIS process (#liveSpawns → in-process path unchanged) or no §14 record, else identity-verified §10.2-ladder signal to the process GROUP. signalVerifiedStrict re-runs full §14 (ps identity + nonce), signals only on verdict==='match'. Never appends child.stopped (owner folds once via transport/T13). Reviewer exploit (terminal run while child mutates) DEFEATED.
- W3-3 metadata redaction: HOLDS (all sinks: run_meta.goal, spec_draft.goal+canonicalSpec, role_round.inputs stringified_json, implement_verify_loop.taskScope, event spec.revise.requested.feedback; field-level registry at the single append+save boundary; no over-redaction; both drivers).
- W3-4 draft atomicity (the credit-halted one): HOLDS — VERIFIED COMPLETE, no half-applied edits. All 3 reqs met (draft+artifact before advance; completion event carries artifact hash+version; crash-injection test at the exact window). handleResume gained the W3-4 coordinator-only unsuspended re-drive (commands.ts ~1089).
RESIDUALS the verifiers found (now in wf_cd99b7b8-ebf):
- W3-5(a) [BROKEN]: cross-process admission TOCTOU. #admitSpawn counts durable others (SELECT) then reserves in-process, but this process's durable child record is written only at spawn → two concurrent CLI processes both see 0 others and both admit (proved: cap=1 → 2 spawned). FIX: atomic count-and-reserve inside one serialized SQLite write txn (BEGIN IMMEDIATE, both drivers); dead-pid reservations reclaimable (no new deadlock).
- W3-1(b) [P0 residual, security]: mutation guard diffs only HEAD+porcelain, so .git/hooks/pre-commit planting, .git/config writes, and new gitignored files evade it. WORST: worktree cwd → `--git-common-dir` resolves to PRIMARY .git → plant a pre-commit hook = persistent undetected RCE on next primary commit. FIX: extend snapshot to primary .git/hooks + .git/config content-hash + `git status --porcelain --ignored`; drift → existing typed verification_runner_violation. Re-attack probe: hook/config/ignored + other-evasion hunt (info/exclude, packed-refs, alt hook names).
- W3-5(d) [partial]: holderless/malformed-identity orphan lock never reclaimed → deadlock. FIX: unreadable-identity lock reclaimable; healthy holder never falsely reclaimed.
- W3-1 documented-not-fixed caveat (verifier-flagged, operator-opt-in): credential-shaped refusal uses SECRET_KEY_NAME_RE which excludes bare auth/key, so SSH_KEY/DEPLOY_KEY/AUTH/BEARER/COOKIE/SESSION_ID pass verification.envAllowlist refusal — documented as heuristic caveat, NOT widened this round (false-positive risk on author/primaryKey; revisit if it becomes load-bearing).
- W3-1(a) env vector + W3-1(c) refusal: HOLD.
Spend rhythm: 3 waves so far this session on Opus (finish-W3 = 788k subagent tokens). Residual round is focused (2 sequential builders + 3 verifiers). After it converges: W4 (config honesty + switch-model + telemetry + F1 restart-safety + F6 supervision + F8 shell-quote), then Codex full-diff review, then HTML refresh.

## 5n. W3 CONVERGED (residual round wf_cd99b7b8-ebf DONE 5/5) — reviews 1 & 2 fully closed + verified
Residual round outcome (tree now 1220 tests / 72 files, tsc clean, index empty, pack ok):
- W3-5(a) BROKEN → FIXED + HOLDS: atomic count-and-reserve inside transactionImmediate (BEGIN IMMEDIATE, both drivers) via new DurableSpawnReservationStore; concurrent cap=1→exactly one, cap=2→exactly two, both drivers; dead-owner reservation reclaimed (no deadlock/no permanent slot loss). Regression proven load-bearing (neutering reserveWithin made it fail).
- W3-5(d) → FIXED + HOLDS: holderless/malformed-identity lock reclaimed after bounded holderlessGraceMs; healthy holder never falsely reclaimed; dead/recycled-pid paths unchanged.
- W3-1(b) P0 residual → FIXED + HOLDS: mutation guard extended to primary .git/hooks manifest + .git/config hash + `git status --porcelain --ignored`, hooks/config located via shared common-dir (closes worktree-cwd→primary hook-planting RCE). All 4 probes caught, independently reproduced in a synthetic repo; pre-fix snapshot evaded all 4 (proves the tests bite). Remaining .git evasions (info/exclude, info/attributes, packed/loose refs, loose objects) confirmed NON-code-execution (hook exec needs .git/hooks=covered; filter/pager/hooksPath exec needs .git/config=hashed) → documented as deliberate residual.
- W3-1(d) doc honesty → the verifier itself corrected PLAN.md L161 (was stale, described pre-fix guard), implementor.ts header, README security section — reviewed by orchestrator, accurate (names the non-exec .git residual + the SECRET_KEY_NAME_RE operator-opt-in caveat). NOTE: a verifier edited a code-file header + docs (benign, doc-only); tree re-verified green after settle.
All FIVE live findings of external review 2 are now closed + adversarially verified. W3-1 P0 sealed incl. the RCE residual the finish-W3 verifier surfaced.
Next: W4. Its one design-heavy item = F1 restart-safety (review 4) → thinker + Codex design pushback FIRST (design draft scratchpad/w4-f1-design.md), then finalize W4 spec, then checkpoint before the build wave. Codex full-diff review of the converged W1-W3 state also queued (or user-triggered /code-review ultra). Waves so far this session on Opus: finish-W3 (788k) + residuals (425k) ≈ 1.2M subagent tokens.

## 5o. F1 restart-safety design pushback (f1-thinker Opus + f1-codex) — CONVERGED; drop P3, use T17, and a pre-existing P1 bug found
Both critiques independently confirmed the finding and CONVERGED on a simpler, safer design than my draft. Key outcomes:
- **NEW PRE-EXISTING BUG (thinker S1, orchestrator-VERIFIED) — reap peer-kill [P1].** `registry.reapOrphans` (registry.ts:335-353) iterates the ENTIRE shared store and SIGKILLs every ps+nonce identity match; `ProcessIdentityRecord` (registry.ts:51-67) has child pid/pgid/nonce but NO owning-orchestrator pid. So a `harness resume R2` in process B (reap is called ONLY from handleResume, commands.ts:1076) SIGKILLs process A's LIVE child of run R1 — an identity-UNambiguous live peer, which the "withhold-on-ambiguity" guard does NOT cover. Violates PLAN §14:139 ("a generation live IN the acting process is never cross-signaled"). Pre-existing (W2-6), NOT introduced by F1, but F1's producer would widen it (mark the killed peer interrupted). FIX (pattern already exists from W3-5): add ownerPid=#selfPid to ProcessIdentityRecord at registration; in reapOrphans skip records owned by a DIFFERENT still-alive orchestrator via the exact #reservationOwnerLive pattern (service.ts:2691-2695: #ps.isAlive + sampleIdentity). Reap only self-owned or provably-dead-owner records. → W4-0, prerequisite for W4-4.
- **Producer (revised P1): wire T17, not T13.** thinker S3: T17 `recovery.running_segment_found` (transitions.ts:433-446) is the PURPOSE-BUILT transition for "orchestrator restart finds running segment → interrupted" (PLAN §12.3:129-130) and is DEAD CODE (emitted nowhere in prod). My draft routed around it with T13 (the child-crash transition) → conflates orchestrator vs child crashes, pollutes RestartBreaker counters + P4b respawn budget. Use T17 BUT it is UNDER-GUARDED (`preconditions:[]`) — must add suspension_in:['none'], phase_non_terminal, child_active, generation_matches_active, else a reap clobbers a paused_limit run or resurrects a terminal run. Condition = isLiveChild (status!=='stopped', catches `spawning` during pin — codex) AND stage-aware (round.stage==='completed' → just confirm child.stopped, no interrupt/counter; pending|active → T17). Rejected trigger = benign no-op, don't throw.
- **Consumer (DROP P3, both agents).** P3 (persist pending verifier round before dispose) is the wrong layer: can't thread implementationCommit (only known after git.resolveSha, AFTER dispose), adds a hot-path write, and MANUFACTURES the Q3 ambiguity. Replace with ONE resume-routing gate in handleResume: `suspension==='none' && round.role∈{implementor,verifier} && round.stage==='completed' && (activeChild===undefined||status==='stopped') && phase non-terminal → driveReentry`, owner-liveness-gated. Subsumes coordinator carve-out + variant 1 + variant 2 + the SYMMETRIC verifier-completed→T23/T24-not-landed gap codex found (crash after verifier round completes but before the merge-readiness probe/transition lands → today re-runs the ENTIRE verifier from scratch). resolveResumeEntry already returns the right next step from the durable stage.
- **P2 = verification only (already wired).** T12→driveReentry→reenterImplementVerify→resolveResumeEntry is role-generic (RESUME_ENTRY_PHASES ⊇ implementing/verifying/needs_remediation); codex traced the exact composition (reap-produces-interrupted within the same resume call → falls to service.resume()=T12). No new code — add a crash-injection test at that composition point. CORRECT the draft's worktree phrasing: implementor dirt is §16.3 WIP-committed-or-reset (partial work PRESERVED), NOT discarded (only the read-only verifier discards) — do not weaken "checkpoint covers all work" (thinker S5).
- **Breaker (Q4): T13-vs-count DISAGREEMENT resolved toward thinker.** codex said reuse T13 + count (conflation "safer"); thinker said T17 + DON'T count orchestrator crashes toward the child breaker. Sided with THINKER (principled: T17 is purpose-built + dead; orchestrator OOM/reboot says nothing about child health; interrupted requires MANUAL resume so no auto-loop to bound; separate interrupt-recovery counter if ever needed). BOTH agree RestartBreaker/evaluateCrash is UNWIRED today (#interruptOnChildDeath fires T13 with no breaker check, service.ts:2013-2022) → W4-1 must wire it into the child-crash site.
- **Q answers:** manual resume (no silent auto-rerun; status shows interrupted+role+§16.3 outcome; --yes for automation); idempotency already sufficient (generation-match + child_active precondition + registry removal), add T17 preconditions + a double-reap test; Q3 dissolves by dropping P3; breaker per above; two mechanisms not P3.
W4 scope now (8 items): W4-0 reap owner-liveness [P1 pre-existing], W4-1 config honesty + wire breaker/cadence + reject non-wait failover, W4-2 switch-model via W3-2 channel + per-role model persistence, W4-3 telemetry aggregate/prune, W4-4 restart-safety (T17 producer + consumer gate + symmetric gap; drop P3), W4-5 supervision (os:darwin declare + watchdog .catch()), W4-6 shell-quote merge cmds, W4-7 [review 5] verification timeout kills descendant process-group, W4-8 [review 5] adapter created before #admitSpawn → Codex temp-home leak on rejection (admit before resource creation / shared cleanup guard). Coupled P1 core = W4-0+W4-4+W4-1-breaker. Checkpoint with user before building (bigger/costlier than W3). Design draft: scratchpad/w4-f1-design.md.

## 5p. W4 restart-safety core (wf_80ce8a59-c81) — DONE 6/6; 1 residual → run-ownership lease
Landed + adversarially verified (1237 tests / 73 files, tsc clean, index empty, pack ok):
- W4-0 reap peer-kill [pre-existing P1]: HOLDS + mutation-proven. registry.ts ProcessIdentityRecord gains ownerPid+ownerStartedAt (=#selfPid at registration, durable both drivers); #ownerLive() gate (isAlive + sampleIdentity startedAt match, recycled-pid safe) skips live-owner records in reapOrphans BEFORE any verify/kill. A live peer's identity-unambiguous child is never killed; dead-owner + recycled-owner orphans still reaped; ambiguous child still withholds. Neutering #ownerLive fails the peer test.
- W4-4 producer: HOLDS. T17 recovery.running_segment_found gained preconditions [suspension_in:['none'], phase_non_terminal, child_active, generation_matches_active] (events.ts added optional generationId); reapOrphanProcesses is stage-aware — dead-owner isLiveChild (incl. `spawning`) generation-matched round: stage===completed → confirm child.stopped (no interrupt/counter), else T17 interrupt. NOT T13 (keeps orchestrator restarts out of the child breaker). Rejected trigger = benign (ingest status 'rejected', no throw).
- W4-4 consumer: HOLDS except one residual. handleResume gained ONE routing gate (suspension=none, role∈{impl,verifier}, stage=completed, activeChild undefined/stopped, phase non-terminal, !isRunClaimedByLiveProcess) → driveReentry. Closes variant 2 (impl done/verifier not dispatched) + the symmetric verifier-completed→T23/T24-not-landed gap. P3 correctly NOT built. Worktree phrasing corrected in PLAN (implementor WIP §16.3-preserved, only verifier discards).
- breaker: HOLDS. evaluateCrash wired into #interruptOnChildDeath (both crash sites) → breaker_open ⇒ T14 restart.exhausted, else T13; T17 (orchestrator crash) never counts; breakerReset (T15) now clears the in-memory window too. Child crash-loop trips breaker_open through the runtime path (didn't before → closes review-3/4/5 F3/breaker-unwired).
- **RESIDUAL [W4-4 consumer, real, narrow, non-regressive]:** isRunClaimedByLiveProcess/runOwnedByLiveProcess queries the per-CHILD registry, but clean dispose removes the child record (service.ts:2856) AND the spawn reservation (:1754). Between an implementor's clean stop and verifier dispatch (a gap of real worktree/git I/O) a LIVE healthy peer holds NO run record → the gate can't distinguish 'owner crashed' from 'owner alive between rounds' → a concurrent `harness resume` in that window would double-drive the worktree (two writers). Non-regressive (the pre-existing W3-4 coordinator gate commands.ts:1096 has NO owner-liveness check at all). FIX = run-level ownership lease: durable {runId, ownerPid, ownerStartedAt} acquired for the DURATION of an orchestrator's active execution of a run (across rounds), released in the outer finally; isRunClaimedByLiveProcess checks THAT (owner-alive → withhold). Reuses the ownerPid/liveness pattern; crash-safe (dead owner → resume proceeds = intended recovery). Also gate the pre-existing coordinator carve-out with it. This lease IS the "durable owner/control channel" recurring across reviews — and the substrate W4-2 switch-model wants. → residual round wf pending.
Then: W4 second wave (config-reject failover, switch-model on the lease, telemetry, supervision os:darwin+watchdog .catch(), shell-quote, verification-timeout process-group, adapter temp-home leak). git: source committed e5cad24 (user pushes); everything since is UNSTAGED.

## 5q. W4-core residual (run-ownership lease, wf_dce03c98-c66) — DONE; stated residual CLOSED; restart-safety core CONVERGED
Built DurableRunOwnershipStore (src/app/run-ownership-store.ts, mirrors spawn-reservation-store keyed by runId {runId,ownerPid,ownerStartedAt,acquiredAt}); acquire at runImplementVerifyLoop outer entry (start-that-drives AND resume-that-drives, held ACROSS rounds), release first in the outer finally; isRunClaimedByLiveProcess now reads the lease (#runOwnershipOwnerLive = isAlive + sampleIdentity.startedAt match, recycled-safe); coordinator carve-out (commands.ts:1096) ALSO gated. 1244 tests / 73 files, tsc clean, index empty. STATED residual (alive-owner between-rounds double-drive) CLOSED + mutation-proven for BOTH the impl/verifier gate and the coordinator carve-out; dead-owner/recycled proceed; lease released on normal/pause/error.
- **Attack (e) — accepted documented residual (verifier-found, out of 5p scope, NON-REGRESSIVE):** two SIMULTANEOUS `harness resume` of the SAME crashed (dead-owner) run both pass `!isRunClaimedByLiveProcess` (owner dead) and both drive → worktree CONTENT double-write. acquire() is last-writer-wins, not CAS; gate-check and acquire aren't atomic across processes. `.git/worktrees` STRUCTURAL corruption is independently prevented by the W3-5 advisory git-op lease (atomic mkdir); only manual-concurrent-recovery content double-write remains. FIX scheduled with W4-2 switch-model (which needs the CAS owner-channel anyway): make DurableRunOwnershipStore.acquire a CAS inside BEGIN IMMEDIATE (accept iff unclaimed OR self OR provably-dead/recycled, §14 liveness inside the txn, return boolean; losers withhold) — mirrors the W3-5a count-and-reserve. Documented in the store doc + PLAN §14 (sequential-reclaim assumption).
RESTART-SAFETY CORE COMPLETE: W4-0 peer-kill fixed, all 5 crash windows resumable (T17), breaker wired, alive-owner double-drive closed. Proceeding to W4b (the P2 remainder) per the user's "then a second wave for the rest"; switch-model (W4-2) HELD for a design pass (owner-channel + CAS).

## 5r. W4b (P2 remainder) launched — config-reject/cadence, telemetry, supervision, shell-quote, timeout-pgroup, adapter-leak
6 items, switch-model (W4-2) deliberately excluded (design-adjacent, needs the run-ownership CAS + owner-channel design). Structure: isolated fixes parallel (W4-6 shell-quote verifier.ts; W4-5 supervision package.json os:darwin + watchdog .catch(); W4-7 verification-timeout process-group in implementor.ts runner) → service.ts-coupled sequential (W4-1 reject non-wait failover + wire CadenceTracker at completed-turn boundary; W4-3 telemetry aggregateWindow scheduled + status raw-sample fallback; W4-8 move #admitSpawn BEFORE Codex adapter/temp-home creation OR shared cleanup guard) → per-cluster verify + mechanical.

## 5s. W4-2 switch-model design pushback — f1-codex received (f1-thinker PENDING); verdict: ship smaller, defer live-apply
f1-codex critique (owner-channel reviewer, reused from F1):
- **FINDING WORSE than draft (orchestrator-VERIFIED):** T19 preconditions (transitions.ts:466-470) = suspension_in:['none'], operation_in:['idle'], child_active:true — NO segmentId check; begin_model_switch reads only payload.fromModel/toModel. So handleSwitchModel's fabricated `${runId}:pending` segment does NOT reliably reject: when a live child is IDLE between turns, T19 ACCEPTS → operation durably=model_switch against a segment nobody holds → nobody calls set_config_option/confirms → next turn.started fold clobbers operation→prompt_turn → request VANISHES silently, CLI reported "applied". (Rejects only when child_active=false.) currentModelFor (commands.ts:1732) is a literal stub (non-coordinator→undefined) — confirms per-role model unpersisted.
- **Proposal-2 hole (stale intent):** intent keyed (runId,role,toModel) with NO generation/round tie-breaker and never cleared on apply → a stale intent from round N silently applies to a LATER round N+1 (arbitrary delay, no operator visibility); two switch calls race last-writer-wins. FIX: key intent with a requestedAtSequence WATERMARK (like checkResumeEligibility staleness) + explicit consume-and-clear event (mirror resume_reentry.completed / foldResumeReentryCompleted); status must show "pending since round N".
- **Boundary seam EXISTS (Q1/Q2):** #buildRoleSession's prompt closure (service.ts:3130-3184) is the ONE funnel for every role's every turn; it already calls #maybeCadenceCheckpoint after turn.completed folds operation→idle. A #maybeApplyPendingModelSwitch(ctx) at that exact call site GUARANTEES never-mid-turn (synchronous in-process). Observation = POLL there (a nudge is only a wakeup optimization; only the owner holds the adapter handle — W3-2 signals can't do set_config_option). Honest caveat: few/long-turn rounds have no latency bound → document "applies at the current round's next turn boundary; want it sooner = cancel+restart".
- **Confirm-by-echo is self-attestation (Q3):** applyRoleModel reads effectiveValue/echoed off the SAME setConfigOption RPC response, not an independent listConfigOptions read → an adapter optimistically reporting "applied" (given #886 flakiness) sails through. FIX: after setConfigOption success, do a SEPARATE listConfigOptions() re-read and require match before recording model.switch.confirmed.
- **CAS not sufficient alone (secondary race):** last-writer-wins acquire → TOCTOU between switch-model's isRunClaimedByLiveProcess check and a concurrent resume reclaiming a stale lease; CAS narrows but the 3b "persist desired model" write + F8 "read at spawn/pin" must share the W3-5a serialized-transaction shape (service.ts:2629-2659), reuse it don't reinvent.
- **Q6 cancel interaction (sharp):** T18 cancel.requested precondition = phase_non_terminal only (no operation check) → cancel_terminal can force operation=idle/phase=cancelled WHILE the owner awaits a slow (10-20s) setConfigOption on a child cancel just terminated → switch-apply must handle "child died from concurrent cancel" as an expected distinct outcome, not a raw crash. T5 limit-during-switch composes IF setConfigOption is wrapped in the classify-first #routeProviderFailure pattern.
- **VERDICT (f1-codex): ship the SMALLER version this wave** = #1 persist per-role effective model + #4 CAS acquire (also closes restart-safety attack-e) + HONEST gate (live run → queue per 3b OR explicit refuse "not supported live; cancel+restart or wait for a pause"; NEVER the fabricated-segment ingest). DEFER live completed-turn application to a follow-up that (a) keys intent by generation/round + consume-and-clear event, (b) wires #maybeApplyPendingModelSwitch into the #maybeCadenceCheckpoint seam with classify-first, (c) upgrades confirm to an independent listConfigOptions re-read. Reasons: live-apply needs the watermark/consume semantics the draft lacks (build-twice risk); echo-trust needs its own decision before durably recording confirmed; W4b concurrently edits service.ts/commands.ts (merge risk).
AWAITING f1-thinker for the second perspective, then synthesize the final W4-2 plan (smaller-now vs full). Status field for pending-desired-model must be distinct from active (Q4).

## 5t. switch-model design FINALIZED (f1-thinker + f1-codex converged) + External review 6 (user, deep) — a foundational concurrency layer surfaces
### switch-model (W4-2) — both critiques converged → SHIP SMALLER, delete the CLI T19 ingest
f1-thinker corrections beyond f1-codex: (S1) the "intent" CANNOT be model.switch.requested — transitionForEvent is 1:1 and ingest ALWAYS drives the row, so appending it DRIVES T19; the intent must be a DISTINCT non-transition record (desired-model row per (run,role,toModel) or model.switch.intent.recorded mapping to no transition). (S2) T19 has NO owner guard — "only owner acts" must be STRUCTURAL (owner = only code path reaching the T19 ingest); a spurious non-owner operation=model_switch also flips the owner's next limit from T4 into T5 forced-successor. (S3, DISAGREES with f1-codex on confirm) the trustworthy echo is setConfigOption's OWN {effectiveValue,echoed} (spi.ts:337-341), NOT a follow-up listConfigOptions (which can report the local optimistic value); the REAL bug is resolveOptionId silently falling back to the first advertised option for an unadvertised target (model-resolution.ts:171-178) → wrong model set + "confirmed"; fix = require effectiveValue===requested toModel AND echoed, REJECT unadvertised targets, echoed:false ⇒ "applied unconfirmed" (parity w/ initial_config_pin), not auto-successor. (S4, NEW — also a shipped-core gap, orchestrator-VERIFIED) acquireRunOwnership is called ONLY in runImplementVerifyLoop (orchestrate.ts:438/639); runCoordination NEVER acquires → isRunClaimedByLiveProcess is FALSE during a live coordinator round → the W4-core coordinator carve-out owner-liveness gate is a PRODUCTION NO-OP. (S6) #1 is a READ-MODEL not new persistence: effective model already durable in child.spawned pins (service.ts:775-781, toChildPinRecords); currentModelFor (commands.ts:1732) just doesn't read them for non-coordinator. 
FINALIZED W4-2 SMALLER-NOW = (1) effective-model READ projection folding child.spawned pins; (2) CAS acquire (closes attack-e); (3) durable desired-model record (distinct, non-transition) + honest gate + DELETE the fabricated-segment T19 ingest (commands.ts:1708-1721); (4) status distinguishes effective(running) vs desired(pending); (5) extend the run-ownership lease to runCoordination (fixes S4, a restart-safety-core residual). DEFER live owner-observe/apply-at-boundary loop (needs S4 + generation/round-keyed intent + consume-and-clear + the #maybeCadenceCheckpoint seam + classify-first setConfigOption + the S3 confirm fix). Own finding: resolveOptionId wrong-model fallback affects initial_config_pin/F8 TODAY, not just switch-model.

### External review 6 (2026-07-19, user) — fresh (73 files/1255 tests, ~current mid-W4b). Triaged:
- **F1 [P1] NEW, FOUNDATIONAL — write-path not atomic (orchestrator-VERIFIED).** appendTriggerWithEffects (write-path.ts:46-87) folds projection.currentState captured by ingest() BEFORE the txn, and does an UNCONDITIONAL db.projections.save with cursor=last event sequence — NO cursor CAS. A concurrent control op (pause/cancel from process B) appending between A's read and A's save is silently overwritten; recover() won't replay the lost event → lost terminal/suspension state. Sits UNDER every gate built so far. FIX: load-validate-append-save under ONE BEGIN IMMEDIATE per run, OR cursor-CAS-with-retry. Design-sensitive (core engine change) → design pushback before build.
- **F2 [P1] = KNOWN cluster:** ownership advisory not exclusive = attack-e CAS + S4 coordinator gap (both already identified) + NEW detail: pending-reentry path bypasses the owner gate (commands.ts:1086). Folds into the switch-model CAS + S4 lease-extension + a gate on the pending-reentry path.
- **F3 [P1] NEW — checkpoint linkage not atomic (needs verify):** pause commits checkpoint event (service.ts:2180) then saves RoleRoundProjection.checkpointRef SEPARATELY (:2190); crash between = event but no resume pointer; cadence checkpoints only append the event (:2330) while resume reads only round.checkpointRef (commands.ts:1502). FIX: atomic linkage OR resolve latest compatible checkpoint from the event log by sequence + assignment/spec binding.
- **F4 [P1] NEW — breaker not durable (consistent with how W4-core built it):** RestartBreaker window/recovery state is in-memory maps; each CLI invocation = fresh service (cli/index.ts:151) → bounds RESET between manual resumes; service.ts:2067 supplies no checkpoint hash; recordProgress() has no caller; generation-less T14 has no preconditions (transitions.ts:391). FIX: persist breaker history durably + generation-check before evaluate/open.
- **F5 [P1] NEW — stale dist/ (release hygiene):** package ships dist/ (package.json:7) which differs from a clean build + is MISSING all run-ownership-store outputs → npm pack would package the OLD impl. FIX: prepack build + CI check. (Moot while pre-release/unstaged, but real for packaging.)
- **F6 [P2]** switch-model = the W4-2 work above. **F7 [P2]** telemetry = W4-3 (in W4b, finishing). **F8 [P2]** admission: adapter-before-admit = W4-8 (in W4b) + NEW detail: limit probes (service.ts:2583) don't call #admitSpawn at all → can exceed maxLiveChildren. **F9 [P2] NEW** stable-contract mismatch: PLAN says `run` defaults to approved spec's proposals (PLAN.md:172) but handleRun exits missing_profiles unless both flags explicit (commands.ts:707).
CONCURRENCY-HARDENING CLUSTER (new, foundational, P1) = F1 write-path atomicity + F2 ownership-CAS/coordinator/pending-reentry + F3 checkpoint linkage + F4 breaker durability. This is a coherent lower layer under all prior concurrency work; F1 especially is a core-engine change warranting design pushback. Sequence: finish W4b → concurrency-hardening wave (design-first) → switch-model smaller-now (depends on F2 CAS + S4) → cleanup (F5 dist, F9 contract, F8 probe-admit).

## 5u. W4b CONVERGED (wf_352cbfaf, resumed wkb35y237 after an infra stall) + switch-model launched
W4b done 9/9, all 6 P2 items hold + regressionProven, mechanical green 1261 tests/73 files, tsc clean: W4-6 shell-quote (mutation-proven), W4-5 os:darwin + watchdog .catch() (both parts), W4-7 verification-timeout process-group (detached descendant killed, W3-1 confinement intact, mutation-proven), W4-1 reject-non-wait-failover + cadence wired at #maybeCadenceCheckpoint(service.ts:3204) + breaker re-confirmed 18/18, W4-3 telemetry aggregate/prune + status raw-RSS fallback, W4-8 admit-before-adapter. (Infra stall on the first run recovered via resumeFromRunId + a partial-edit-awareness note on W4-3.)
switch-model wave LAUNCHED (wf_d4256c50-27f): Stage1 ownership-exclusive (CAS acquire returns win/lose bool + driver refuses on lose + acquire-before-worktree every path + extend lease to runCoordination [S4] + gate pending-reentry [F2] → closes attack-e/S4/F2) → Stage2 switch-model honest (delete fabricated-segment T19 ingest, effective-model read projection over child.spawned pins, durable desired-model record + honest gate, status effective-vs-desired, resolveOptionId reject-unadvertised) → verify. Live in-place switch DEFERRED.
NEXT after switch-model: the F1-F4 concurrency-hardening cluster (user-sequenced last): F1 write-path atomicity [foundational, design-first], F3 checkpoint linkage atomicity, F4 breaker durability across CLI processes. Then cleanup: F5 stale-dist (prepack+CI), F9 run profile-defaults contract, F8 limit-probe admit-gate.

## 5v. switch-model wave CONVERGED (wf_d4256c50) + F1-F4 design pushback launched
switch-model wave done 5/5, both stages regressionProven, both verdicts HOLD, mechanical green 1287 tests/75 files, tsc clean:
- Stage1 ownership-exclusive HOLDS (all 5 probes): CAS acquire (transactionImmediate, both drivers) returns win/lose bool + accepts iff unclaimed/self/dead-recycled; loser driver throws RunOwnershipConflictError BEFORE worktree I/O; acquire-before-worktree every path; runCoordination acquires+releases (S4 no-op FIXED, coordinator carve-out now real); pending-reentry gated (F2). → closes attack-e + S4 + F2.
- Stage2 switch-model honest HOLDS (all 5): fabricated-segment T19 ingest DELETED (no CLI-produced operation=model_switch); effectiveModelsFromPins read-model over child.spawned pins (currentModelFor real for impl/verifier); new DurableDesiredModelStore + honest gate (no-live-child→pending-at-next-spawn / live→queued; never no-op/segment); status shows effective-vs-desired distinctly; resolveOptionId rejects unadvertised explicit targets (UnadvertisedModelTargetError) — closes the wrong-model-confirmed bug affecting F8. Live in-place switch DEFERRED as designed.
CURRENT TRACK (W4b + switch-model) COMPLETE. F1-F4 concurrency-hardening design pushback launched (f1-thinker + f1-codex, reused): F1 write-path atomicity (A: one BEGIN IMMEDIATE read-validate-append-save vs B: cursor-CAS+retry), F3 checkpoint linkage (atomic-pointer vs derive-from-log), F4 breaker durability (durable state across fresh-per-CLI service + T14 generation guard). Draft: scratchpad/f1-f4-design.md. Checkpoint with user before the BUILD (foundational core-engine change).

## 5w. F1-F4 design pushback — f1-codex received (f1-thinker PENDING); orchestrator-verified
f1-codex critique (exceptional, code-grounded):
- **F1 CONFIRMED + PERMANENT (not stale-until-recover).** Exact interleaving: process A (T13 crash) + process B (T18 cancel) both read pre-crash state untransacted; B commits cancel_terminal first (phase=cancelled); A commits T13 second with HIGHER sequence, folds STALE pre-cancel state → overwrites B's row with phase=implementing/suspension=interrupted, cursor past BOTH events. recover() (fromSequence=cursor+1) can NEVER self-heal → the user's cancel is silently undone, run resumable again. The T18 evidence sits un-replayable behind the watermark.
- **SECOND call site VERIFIED (service.ts:2210-2235): #pauseForLimit** duplicates the identical read(2210)→validate(2212)→appendTriggerWithEffects(2235) bug — the flagship limit-pause path. F1 MUST be ONE shared primitive (transactionImmediate wrapping read+validate+append) replacing BOTH ingest()'s inline seq (1154-1174) AND #pauseForLimit's slice — both slices are fully SYNCHRONOUS (no await inside) → clean to move inside the txn. ORCHESTRATOR NOTE: audit ALL read→appendTriggerWithEffects sites (advanceWorkflowPhase 1223-1238 is a likely third) — route every one through the shared primitive so copy-paste can't reintroduce it. Boundary: does NOT cover RoleRoundProjection #saveRoleRound (no reducer/cursor, last-write-wins) — that's presumed protected by the F2 lease (already landed); keep explicit.
- **F1 = Approach A (BEGIN IMMEDIATE), not B.** B's retry BOUNDARY is easy to draw wrong → duplicated side effects (checkpoint fsync/child touch) if it re-enters past the pure fold. A reuses the tested transactionImmediate (W3-5a), transitions are microsecond-fast, MVP concurrency low (bottleneck = provider round-trips), DB-wide serialization negligible.
- **F3 = derive-from-log (Approach B), harder than "or both".** The resume path (reenterImplementVerify ~1470) TRUSTS round.checkpointRef, does NOT derive (the 2242 comment is aspirationally wrong). Approach A alone doesn't fix cadence checkpoints (they only ingest the event, never touch checkpointRef → invisible to resume). B fixes all current+future checkpoint sites at once + matches codebase philosophy (recover() re-derives EngineState; latestT23FixRequests scans listByRun backward). TRAP: "latest by sequence" alone is unsafe — a higher-seq checkpoint.recorded can belong to a SUPERSEDED spec binding (post spec-revise) or different round; the derivation MUST reuse checkResumeEligibility's binding-equality chain (assignment open+non-stale, spec-hash chain), not a bare max-seq scan. checkpointRef stays best-effort cache hint only.
- **F4 — two new bugs beyond the draft.** (1) T14 VERIFIED unguarded (transitions.ts:394 preconditions:[]) vs T13's suspension_in:['none']+child_active+generation_matches_active (374-377); T14 payload carries no generationId. A stale/delayed restart.exhausted opens the breaker even after a new generation / terminal phase — worse than T13's case (T14 needs explicit T15 reset). Fix: thread generationId RestartAttemptInput→T14 payload (from ctx.generationId at the T13 call site 2122-2129) + give T14 T13's guards (conformance regen). (2) recordProgress() unwired = latent FALSE-POSITIVE: evaluateCrash max_elapsed_recovery measures from recoverySequenceStartedAt (set only on an assignment's FIRST crash, never reset w/o T15) → an assignment that crashes once, recovers, runs clean >maxElapsedRecoveryMs (default 30min, within one round's turn timeout) trips the breaker on its NEXT unrelated crash. Wire recordProgress at a durable stable point (round stage→completed / fresh checkpoint hash). Durable window state: DON'T reuse EngineState.counters.restartsInWindow (monotonic, never decays → collapses window into a lifetime cap); persist restart TIMESTAMPS (T13 occurredAt) in a bounded/pruned array, RestartBreaker hydrates lazily per-assignment in a fresh process — synergistic w/ F3's log-derivation (serves the no-progress "last checkpoint hash").
- **Build order F1→F3→F4.** Honest residuals after: breaker DECISION still from a non-transactional #loadEngineRecord (2128) before evaluateCrash → two concurrent crash-detections could both decide 'restart' → bounded cap overshoot (rare, self-limiting, discouraged by guard+lease); F4 relies on F2 for two-live-owners; F3 only as safe as its binding filter.
AWAITING f1-thinker, then synthesize final F1-F4 plan + CHECKPOINT with user before the (foundational) build.

## 5x. F1-F4 design FINALIZED (f1-thinker + f1-codex converged; disagreements resolved)
f1-thinker corrections beyond f1-codex:
- **F1 is worse than lost-update — ILLEGAL-APPEND.** A's event at N+2 was validated against STALE state@N, so an illegal transition (e.g. normal transition after a cancel) is durably in the log; even rebuild-from-0 folds a contradictory event. Confirms A: validate INSIDE the txn against fresh state. Q2/Q4 clean: applyTransition pure; LimitPausedError thrown AROUND the fold not inside ingest; ingest never called within an open txn (IMMEDIATE always outermost). B's real risk = OUTCOME-FLIP (caller sees 'rejected' on retry after first 'applied'), not double-run.
- **F3: enrich the payload.** checkpoint.recorded today = {checkpointId, artifactHash, reason, segmentId?} — binding-POOR. ENRICH with {assignmentId, specHash, round, role} so the log alone is filterable (no N artifact loads). Resume resolves latest checkpoint.recorded filtered by CURRENT approved specHash (superseded-spec can't resurrect) + assignment non-stale (reuse checkResumeEligibility chain). A cadence checkpoint later-by-seq than a verifier one regresses EVIDENCE (re-verify) not CORRECTNESS — safe direction. checkpointRef = optional cache, don't make atomic.
- **F4 T14 CRUX (orchestrator-VERIFIED, transitions.ts:632): generation_matches_active returns undefined (PASSES) for an unstamped generationId.** So adding the check to a still-generation-less T14 = ZERO protection. Fix MUST STAMP the generation on restart.exhausted (from ctx.generationId at the T13 site 2136-2140) AND add preconditions [generation_matches_active + phase_non_terminal + suspension_in:['none']]. (T14 buckets by assignment so cross-generation windowing is correct; it's the APPLICATION that must not clobber a moved-on/paused/terminal run.) Conformance regen.
- **F4 durable-window CORRECTION — DEFER to P4b.** The LIFETIME cap IS durable (evaluateCrash reads counters.lifetimeRestarts @2128, trips at 10 cross-process) → crash-loop bounded at 10, NOT infinite (draft/f1-codex overstated). Only the FAST window (5/10min) + no_progress + max_elapsed are process-local. And P4a has ZERO auto-respawn (T13⇒interrupted, manual resume) → human-paced, lifetime governs, window is the wrong tool; in-memory window is FINE until P4b's single-process rapid respawn. If ever made durable: fold the restart-occurredAt list INTO EngineState.counters, prune in the pure T13 reducer by event occurredAt (ONE write, rides F1's atomicity) — NEVER a separate store (a 2nd write = a new crash window, the class F1 closes).
- **no_progress half-wired:** latestCheckpointHash never passed to evaluateCrash (2122-2129) → no_progress can't fire; recordProgress() no caller → max_elapsed_recovery measures from the FIRST crash, never reset → false-positive breaker on an unrelated later crash >30min. Resolve: WIRE it (latestCheckpointHash from F3's log-derivation + recordProgress at round stage→completed — synergistic with F3) OR honestly disable until wired. Lean: wire via F3.
DISAGREEMENTS RESOLVED: F4 durable-window → DEFER to P4b (f1-thinker, sharper/code-grounded); no_progress → wire via F3's derivation (or honest-disable); T14 needs the STAMP not just the check (f1-thinker crux).
FINAL F1-F4 PLAN: **F1(A)** shared transactionImmediate primitive (read+validate+append) replacing ingest()(1154-1174) + #pauseForLimit(2210-2241) + audit advanceWorkflowPhase(1223-1238); nested db.transaction→savepoint; excludes #saveRoleRound (F2 protects). **F3(B, ∥ F1)** enrich checkpoint.recorded payload + resume derives latest binding-compatible checkpoint from the log (specHash+assignment filter); checkpointRef=cache. **F4(after F1)** stamp+guard T14 (conformance regen) + wire/disable no_progress; durable-window DEFERRED to P4b. Build order F1 → {F3 ∥ F4-T14-guard} → F4-no_progress. Residuals: breaker-decision bounded-overshoot (rare/self-limiting); F4 relies on F2 for two-live-owners; F3 only as safe as its binding filter. → CHECKPOINT user before build (foundational).

## 5y. F1-F4 CONVERGED (wf_0540df07) — foundational concurrency/durability layer done
7/7 agents, all 3 builders regressionProven, all 3 verdicts HOLD, mechanical green 1305 tests/76 files, tsc clean:
- F1 HOLDS: #atomicEngineWrite(runId, build) primitive = #loadEngineRecord + build(applyTransition) + appendTriggerWithEffects inside ONE transactionImmediate (read INSIDE the lock → incompatible trigger REJECTED, never appended folding stale state). All 4 sites routed (ingest ×2, advanceWorkflowPhase, #pauseForLimit); new AppendTriggerOptions.alreadyInTransaction flag lets appendTriggerWithEffects join the outer IMMEDIATE (no nested BEGIN/savepoint); resume + completeCoordinationRound converted to transactionImmediate (outermost). #saveRoleRound left OUT (F2-lease protected, documented). Cross-process T13-vs-T18 interleave: serializes, validated against committed state, NO illegal append, from-zero replay == projection. Mutation-proven both drivers.
- F3 HOLDS: checkpoint.recorded enriched (specHash required + role/round/assignmentId); resolveResumeCheckpoint(runId) reuses checkResumeEligibility then scans listByRun for latest-by-seq matching assignment+specHash; reenterImplementVerify uses the derivation (checkpointRef = cache hint only). Crash-between-event-and-pointer recovers; cadence checkpoint visible; superseded-spec excluded (mutation-proven).
- F4 HOLDS: restart.exhausted stamped w/ generationId; T14 guarded [suspension_in none, phase_non_terminal, generation_matches_active] (conformance auto-adapted, 137 tests) — stamp load-bearing (guard no-ops on unstamped); durable window folded into EngineState.counters.restartWindow, pruned in the PURE T13 reducer by occurredAt vs EngineBounds.windowMinutes, ONE write on F1's atomic append; RestartBreaker hydrates lazily per-assignment (fast window survives fresh-per-CLI service); no_progress wired (latestCheckpointHash + recordProgress at stable point resets recovery seq). lifetime cap unchanged.
Committed after this note. ALL SIX external reviews' findings now closed + verified (or explicitly deferred: live model-switch → P4b; durable-window was INCLUDED per user). Remaining: review-6 cleanups (F5 dist-prepack, F9 run profile-defaults, F8 limit-probe admit-gate) → then P4b/P4c/P5 roadmap. Overnight autonomous: commit each verified wave (never push), stop on a broken wave or a genuine design fork.

## 5z. Review-6 cleanups DONE (wf_e0a3de3f) — ALL SIX REVIEWS CLOSED
F5/F8/F9 hold + regressionProven, mechanical green 1312 tests/76 files, tsc clean:
- F5 dist-prepack: package.json prepack=npm run build → npm pack rebuilds current-source outputs (the missing run-ownership-store outputs now ship). HONEST RESIDUAL (non-blocking, documented in PLAN §P5 + README): tsc doesn't clean dist first, so an orphaned .js from DELETED source could still be packed — recommended CI "dist matches clean build" diff is documented, NOT implemented.
- F9 run profile-defaults: resolveRunProfile — explicit flag wins, else the approved spec draft's proposed profile, else clear missing_profiles exit-2. Honors the PLAN §CLI contract. Mutation-proven.
- F8 limit-probe admit-gate: #executeProbeSession calls #admitSpawn (atomic count-and-reserve) BEFORE adapter/CODEX_HOME create, releases on every path; probes can no longer exceed maxLiveChildren. H-1 intact. Mutation-proven.
STATE: all 6 external reviews' findings closed+verified (live model-switch deferred → P4b by design). git: e5cad24 (source) → b26df5e (reviews 3-6) → cb4d3cb (F1-F4) → [cleanups commit]. main ahead of origin/main, UNPUSHED (user pushes).
OVERNIGHT NEXT: (1) refresh the HTML report to the all-reviews-closed state; (2) HOLD at P4b (failover + live model-switch + real respawn) — a new phase with genuine design forks; draft its design + reviewer pushback so it's ready, but DO NOT build it autonomously (leave the go for the user). Known deferred backlog for P4b+: live in-place model-switch (owner-observe/apply-at-#maybeCadenceCheckpoint + generation-keyed intent + independent listConfigOptions confirm); real bounded respawn (P4b); F5 CI dist-diff; the honest residual breaker-decision bounded-overshoot.

## 5aa. P4b KICKOFF (user decisions 2026-07-20) — design pushback launched
User P4b decisions: (1) on LIMIT → pause + ALERT (default failoverPolicy stays `wait`); (2) on CRASH → ALERT + bounded AUTO-RESPAWN; (3) cross-harness model switch on limit is IN scope as an opt-in failover. Full scope chosen (incl. cross-harness).
Cross-harness answer (given to user): NOT a live in-session switch (can't reconfigure a Codex session into Claude) — it's a checkpoint-linked SUCCESSOR (pause → harness-agnostic mechanical checkpoint → spawn successor on the OTHER harness pinned to target model → adopt worktree → resume). Machinery mostly exists (pauseForLimit checkpoint, generation-tracked successors, worktree adoption, run-ownership lease, T5 re-assertion, F3 log-derivable checkpoint). Tradeoff: successor continues from the MECHANICAL checkpoint, not the dead session's raw history (never crosses ACP) — verifier still gates criteria so correctness holds.
P4b design draft: scratchpad/p4b-design.md — P4b-1 alerts (durable alert.raised + status + pluggable Notifier seam; delivers the existing notify effects), P4b-2 bounded auto-respawn on crash (T13 change: crash→respawn-successor if within the durable F4 breaker window, else breaker_open; config autoRespawn bounded|off, default bounded), P4b-3 opt-in switch_model/switch_harness failover on limit via checkpoint-successor (bounded ladder; failover does NOT feed the crash breaker). Design pushback SENT to f1-thinker + f1-codex (8 open Qs: alert crash-safety, respawn transition shape, successor crash-windows, generation-idempotency, failover target ladder, breaker-vs-failover accounting, cross-harness checkpoint sufficiency). Then synthesize → build (design-heavy, esp. the successor crash-safety) → adversarial verify → commit. Build order P4b-1→2→3, each committed green.

## 5bb. P4b design pushback — f1-codex received (f1-thinker PENDING); reshapes P4b SMALLER
- **HEADLINE (orchestrator-VERIFIED): NO successor-spawn code exists.** segment.successor.required (transitions.ts:988) + notify.requested (transitions.ts:1189/1202) are ONLY emitted, ZERO consumers. T5's "successor required" has NEVER spawned one since P4a. So P4b BUILDS the successor mechanism (not extends) — build ONCE, shared across T5/P4b-2/P4b-3. notify.requested is silently dropped today → P4b-1 alerts fixes a LIVE gap independent of respawn/failover.
- **CORE ARCHITECTURE (elegant): self-drive the existing crash-safe resume machinery.** Don't build respawn/failover as new domain concepts. `interrupted` (T13) and `paused_limit` (T4/T16) are ALREADY crash-safe resume targets (the whole P4a/F1-F4 body). Respawn/failover = the OWNING process self-drives reenterImplementVerify/resolveResumeEntry/adoptWorktree IMMEDIATELY in-process (instead of waiting for a human `resume` or the probe ladder), targeting a different generation/model/harness. Crash-safety INHERITED FREE: a crash mid-respawn leaves the run in the exact durable state (suspension=interrupted/paused_limit, maybe activeChild='spawning') that reapOrphanProcesses+resume already reclaim, unmodified.
- **P4b-2 auto-respawn:** AMEND T13's EFFECT (richer payload respawnEligible{attempt,boundedUntil} stamped by #interruptOnChildDeath when advice.kind==='restart' && config autoRespawn==='bounded') — NO new transition, NO new suspension kind (activeChild='spawning' already means "attempt in flight"). In-process retry wrapper in runImplementVerifyLoop re-drives the existing re-entry; bounded by the breaker counters (each attempt increments; stops at breaker_open→T14 as today). autoRespawn=off reproduces P4a exactly. Only new user-visible bit: status "interrupted — auto-respawn N/5". (f1-codex considered + REJECTED a separate respawnPending marker — interrupted IS the marker; a parallel machine reopens "which pending wins".)
- **P4b-3 failover:** same pattern targeting paused_limit; #pauseForLimit UNCHANGED (already atomic). After it lands, owner self-drives re-entry with target from a per-assignment ladder, recorded via DurableDesiredModelStore.set (Q8: reuse — already {runId,role,harness,model,effort}). Owner dies before self-drive → durably paused_limit, degrades to plain wait for this incident (STATE explicitly: degrades-to-wait, not silent-drop).
- **ONE shared self-drive-successor primitive** for T13/T4/T16/T5; build it in P4b-2 with T5's gap-close in mind (not impl/verifier-specific). Reuse #pauseForLimit unchanged for the pause half.
- **Alerts (P4b-1):** durable alert.raised + best-effort sink + at-least-once via LOG-DERIVATION (scan alert.raised w/o matching alert.delivered — NOT a separate cursor projection, that's the atomic-pointer shape F1/F3 eliminated). CLI/stderr/status --json + pluggable Notifier seam. Ship FIRST + independently.
- **Q-answers:** (4) idempotency already covered (generation_matches_active + child_active fresh-read + lease exclusivity → no concurrent same-assignment crash-handling); DISCIPLINE: re-read state via F1 primitives each self-drive step, never cache currentState. (5) failover ladder = per-assignment bounded, NO implicit default; relax the schema refusal to accept switch_model/switch_harness ONLY with a non-empty ladder; record lineage for provider-split spend. (6) failover doesn't feed the breaker, but a failover successor that later CRASHES SHOULD (assignmentId-keyed) — deliberate. (7) checkpoint-only, no NL digest (deferred §12.2); document tradeoff. (8) self-drive inside the lease-holding process; never re-acquire ownership in the successor (only the resume fallback does).
- F1-F4 VALIDATED by f1-codex (primitives correct: #atomicEngineWrite, resolveResumeCheckpoint, CAS ownership, T14 generation+phase-guarded, #interruptOnChildDeath feeds evaluateCrash the F3 hash).
- BUILD ORDER P4b-1 → P4b-2 (build shared primitive) → P4b-3. AWAITING f1-thinker → synthesize → build.

## 5cc. P4b design FINALIZED (f1-thinker + f1-codex converged) — self-drive spine; alerts+respawn first, failover as fast-follow
Both agree: successor-spawn ACTOR is unbuilt (verified: segment.successor.required/notify.requested zero consumers); auto-respawn is CONTROL-FLOW not a new transition; alerts greenfield; F1-F4 primitives validated correct.
ARCHITECTURE: respawn/failover = the lease-holding owner self-drives the EXISTING crash-safe resume machinery (reenterImplementVerify/resolveResumeEntry/adoptWorktree) in-process immediately, targeting a different generation/model/harness. NO new suspension kind (activeChild='spawning' already = attempt-in-flight). Crash-safety mostly inherited.
SUCCESSOR SPINE (the real unbuilt core, build ONCE for T13/T4-T16/T5): f1-thinker's window analysis wins over f1-codex's "interrupted is enough" — add a durable successor-INTENT marker (resumeReentryPending sibling) written ATOMICALLY via #atomicEngineWrite {crashed-gen→stopped, window-fold, seed checkpoint hash, target model/harness} BEFORE the OS spawn; existing spawn discipline (child.spawn.initiated=spawning → registerSpawnSupervision → spawn → child.spawned ACKS the marker) + reaping kill a mid-spawn orphan + re-drive an unacked marker → exactly one successor. Crash windows A(pre-marker→re-decide) / B(post-marker,mid-spawn→reap+re-drive) / C(successor crashes→bounded) — adversarially test FIRST.
P4b-1 ALERTS: durable alert.raised FOLDED into the triggering transition's #atomicEngineWrite (alert can't exist without its cause); at-least-once delivery, unacked DERIVED from log (alert.raised w/o alert.delivered; f1-codex over f1-thinker's cursor-projection — consistent w/ F1/F3 derive-don't-point; optional cursor cache); sinks stderr+status --json, Notifier interface seam only. Reuse notify effects as single emit point. Ship FIRST + independently (fixes live silent-drop gap).
P4b-2 AUTO-RESPAWN: **#interruptOnChildDeath RETURN advice not THROW (S3, service.ts:2127/2162)** → runImplementVerifyLoop loops back + re-spawns. restart advice + autoRespawn=bounded + owner-alive → auto resume(T12)+successor after backoffMs; gate respawn on the GENERATION-MATCHED T13 (not evaluateCrash's gen-blind advice, S4). **S5 FIX (F4 residual, verified breaker.ts:240-249): evaluateCrash must DECIDE off the durable restartWindow each call, deque=cache only** — else a stale-gen crash mis-gates respawn. autoRespawn=off = P4a exactly; breaker_open→T14; orchestrator-crash→T17 (breaker-exempt, manual). status "auto-recovering (attempt N)". Same-harness/model.
P4b-3 FAILOVER (deferred to wave 2): reuse spine+desired-model store; per-assignment ORDERED bounded ladder {harness,model}, NO default (unset=wait), relax schema refusal to accept switch_* ONLY with a non-empty ladder; failover successor keeps SAME assignmentId (S4-i, else breaker-evasion) + OWN per-incident ladder counter (S4-ii; T25 only covers all-limited-now not oscillation); never routes through evaluateCrash but a failover successor's later CRASH feeds the breaker (deliberate); owner-dies-pre-self-drive → durably paused_limit, degrades-to-wait (state explicitly); cost lineage for provider-split spend; cross-harness checkpoint-only (no NL digest, deferred). switch_model before switch_harness.
SEQUENCING: reviewers strongly recommend WAVE 1 = alerts + successor spine + auto-respawn (same-harness) — fully delivers the user's crash decision — then WAVE 2 = failover on the PROVEN spine. (User picked "include failover"; this still delivers it, immediately after, on a proven spine — not a drop. Proceeding with wave 1; will fold failover in next.) Regenerate §6.3 conformance only where the table changes (likely just new alert/successor supporting events).

## 5dd. P4b wave 1 PARTIAL (wf_787bc3e3) — alerts + S5 landed; successor spine deliberately deferred
Mechanical green 1324 tests/77 files, tsc clean, index empty. Landed + verified:
- P4b-1 ALERTS: done, verdict HOLDS. alert.raised folded as a supporting event into the trigger's #atomicEngineWrite (no cause→no alert; rejected triggers append no alert; alertId derived from trigger key = replay-stable; detail redacted §17.1). Delivery = at-least-once, un-acked DERIVED from log (alert.raised w/o alert.delivered per sink, no cursor as source of truth), retried on restart. Sinks: StderrNotifier + StatusJsonNotifier + Noop seam (no network). status --json gains alerts[]. Fixes the LIVE silent-drop (notify.requested never delivered). Files: src/domain/alerts.ts, src/app/alerts.ts(+test), events.ts, ids.ts, service.ts (#deriveAlertEvents, #ingestTransition), commands.ts.
- S5 fix: done + verified (breaker.ts). evaluateCrash now DECIDES off the durable reducer-pruned counters.restartWindow each call (#prunedDurableWindow), in-memory deque demoted to a re-hydratable cache; #hydrateWindow once-seed removed. Mutation-proven (old accumulator → wrong breaker_open). Closes the F4 residual before respawn depends on it.
- **SUCCESSOR SPINE: builder returned BLOCKED — DELIBERATE (sound judgment).** It declined to half-build a crash-safety-critical mechanism in the same pass, shipped the clean S5 increment, and recommended a focused build with the confirmed REUSE POINTS: resume machinery reenterImplementVerify/resolveResumeEntry/adoptWorktree (flows/orchestrate.ts + cli/commands.ts:1516), the marker-ack path (service.ts:1964), resolveResumeCheckpointHash (service.ts:1648). Halt-guard then skipped auto-respawn. → committing alerts+S5, then a FOCUSED spine wave (spine alone, heavily crash-tested at windows A/B/C, then auto-respawn on it), per §5cc.

## 5ee. P4b successor spine + auto-respawn DONE + VERIFIED (wf_e7ffea1a, focused rebuild) — crash decision fully delivered
Both builders done+regressionProven, both verdicts HOLD (all crash windows), mechanical green 1349 tests/79 files, tsc clean:
- SPINE HOLDS (11 claims): durable successorIntent marker (state.ts sibling of resumeReentryPending) written in ONE #atomicEngineWrite by fusing into the T9/T12 initiate_resume fold — carries {target {harness,model,effort}, seed checkpoint hash from resolveResumeCheckpointHash, reason}, committed BEFORE any OS spawn; eligibility inside the write-locked txn. Post-commit the EXISTING spawn discipline re-enters (reenterImplementVerify/resolveResumeEntry/adoptWorktree→child.spawn.initiated→child.spawned); child.spawned ACKS the marker (foldResumeReentryCompleted clears BOTH markers idempotently; only other clear = cancel_terminal). Windows A(pre-marker→paused, re-decide, no orphan)/B(post-marker,mid-spawn→un-acked marker+reap→EXACTLY ONE successor)/C(post-spawn→ordinary gen-matched T13) all HOLD on BOTH drivers via crashOnAppendOf injection. stale-gen no duplicate; lease-holder-only spawn; re-read state each step; honest refusals. T5 GAP CLOSED (segment.successor.required now drives the spine). §6.3 rows UNCHANGED (only an optional T9/T12 payload field + conditional fold branch). Mutation-proven (revert marker-clear → 10/14 spine tests fail).
- AUTO-RESPAWN HOLDS (5 claims): #interruptOnChildDeath returns a CrashDisposition (not throw); the two crash call sites throw AutoRespawnSignal or the sink-safe error via #unwrapCrashDisposition; runImplementVerifyLoop catch→waitMs(backoffMs)→recordSuccessorIntent→re-drive. Crash-loop auto-respawns to the breaker bound then breaker_open (no infinite loop); autoRespawn=off reproduces P4a EXACTLY; orchestrator-crash→T17 (breaker-exempt); stale-gen no respawn. Config autoRespawn (bounded|off, default bounded). Mutation-proven (force respawn off → 4 tests fail → P4a). Same-harness/model; spine is GENERAL (recordSuccessorIntent takes a target) so failover wave 2 is a delta.
Committed after this note. USER'S CRASH DECISION (alert + bounded auto-respawn) FULLY DELIVERED. NEXT = FAILOVER wave 2 (the user's limit-escalation / cross-harness, on the proven spine): switch_model then switch_harness via recordSuccessorIntent with a ladder target; per-assignment ORDERED bounded ladder {harness,model}, NO default, relax schema to accept switch_* only w/ a non-empty ladder + own per-incident ladder counter (T25 only covers all-limited-now); same assignmentId (else breaker-evasion); failover never feeds evaluateCrash but a failover successor's later crash does; degrades-to-wait on ladder exhaust; cost lineage for provider-split spend; cross-harness checkpoint-only. Design already reviewed in §5cc.

## 5ff. External review 7 (2026-07-20, user) — MID-failover-wave snapshot (1357 tests, dirty tree incl untracked failover-store.ts). Triage (verify on the SETTLED post-failover tree; do NOT read mid-wave = moving target):
FAILOVER-SCOPE (re-verify after wf_49793aa1 settles — the routing agent is still building; findings may be partly addressed or survive → residual fixes):
- F2 [High] failover advancement NOT atomic (service.ts:1852): desired-model + ladder-position + alert + successor-intent are SEPARATE writes; counter incremented BEFORE intent so a crash-retry skips a rung. FIX = ONE atomic failover-advance transition (position+intent+alert together, spawn post-commit) — the F1 discipline the wave was told to follow but may have violated in routing.
- F4 [High] config accepts unhonrable values (schema.ts:91/363, model-resolution.ts:43): arbitrary harness/effort strings parse ({harness:"claude-code",effort:"xhigh"}); switch_model only requires ladder entries match EACH OTHER, not the RUNNING harness → silent cross-harness switch. FIX = validate against runtime enums (harness claude|codex, effort vocab) + enforce switch_model stays on the role's effective harness at dispatch. (My config prompt said "same harness" but reviewer says it only checks entries vs each other.)
- F8 [Med] limit status contradicts durable state (commands.ts:1627/1701/2027): always reports policy `wait` even under switch_*; reports ETA unknown when the incident has an exact structured resumesAt. FIX = status reads actual policy + surfaces structured resumesAt.
- F1 [High] VERIFIER HALF is failover too (verifier failover re-enters implementor branch).
NOT failover-scope — PRIORITIES:
- **F1 [High] — auto-respawn/failover re-enter the WRONG role (orchestrate.ts:470/690). Bug in COMMITTED bfd859e auto-respawn.** A verifier LimitPausedError/AutoRespawnSignal returns to loop top with phase=verifying but skipImplement=false → enters implementor branch → the guard throws LoopCompositionError. So verifier-side auto-respawn AND failover are BROKEN; my auto-respawn verifier only tested an implementor crash. FIX = the re-drive must carry the failed ROLE and re-enter at verification (resolveResumeEntry already returns first:'verify' for a completed-implementor/verifying phase — the auto-respawn loop must USE it instead of restarting the implementor branch).
- **F3 [High] NEW DEEP — non-limit active-round error strands the run (service.ts:2336/2379, commands.ts:1157).** A caught auth/protocol/budget/local-flow/git/artifact error on an ACTIVE round skips the `completed` write but records child stopped → unsuspended implementing/verifying, no completed → resume (completed-boundary-only) AND run (approved-only) both REFUSE. Same class as the original restart-safety F1 but for CAUGHT non-limit errors. FIX = a durable failed/interrupted outcome for a stopped active round + resume reclaims stopped pending/active rounds.
OPERATIONAL (Medium/Low):
- F5 [Med] workspace identity not pinned at creation (commands.ts:382, index.ts:252, manager.ts:229): relative workspace path resolved from the run-process cwd; base commit resolved at run not approval → source drift between approval+execution silently changes the base. FIX = resolve+pin absolute workspace + base commit at start/approval.
- F6 [Med] checkpoint cadence PROCESS-LOCAL (service.ts:1047/3140 in-memory map resets across start/run/resume processes — same class as the F4 breaker-window residual) + pre_model_switch/pre_verify_handoff boundaries UNWIRED. FIX = durable cadence counter (fold into a projection) + wire the two boundaries.
- F7 [Med] worktree+artifact cleanup UNREACHABLE (orchestrate.ts:715 leaves every worktree; manager.ts:401 removeWorktree never called; store.ts:209 gcSweep no caller; FS GC wouldn't remove SQL metadata). FIX = wire cleanup at merge/terminal + gcSweep + SQL-metadata GC. (Partly by-design — user reviews unstaged tree — but long-run accumulation is real.)
- F9 [Low] CLI: start-failure omits the generated runId + no `list` command to rediscover; package bin = harness-orchestrator but help/docs say `harness` (commands.ts:382/233, package.json:7). FIX = surface runId on start failure + a list command + fix the bin name.
PLAN: let wf_49793aa1 (failover) settle → re-verify F2/F4/F8/F1-verifier on the settled tree → then a fix wave: F1 (role re-entry, committed+failover) + F3 (non-limit strand) [both High, priority] + the failover residuals + operational F5/F6/F7/F9. The new failover driver funcs currently have NO direct tests (reviewer) — the wave's own verifier will add them.

## 5gg. Failover landed (wf_49793aa1, 1369/80 green, verdict HOLDS on ladder/assignmentId/cross-harness/degrade) but UNCOMMITTED — review-7 cluster fixed on top first
Failover works (driveFailoverOnLimit self-drives the spine with a ladder target; DurableFailoverStore; switch_model same-harness + switch_harness cross-harness Codex→Claude from checkpoint; bounded min(ladder,maxFailoversPerIncident), mutation-proven no-oscillation; same assignmentId; T25 degrade-to-wait + failover_exhausted alert; failover-successor crash feeds the breaker). BUT review-7 findings live in THIS code (orchestrator-VERIFIED on settled tree): F1 (orchestrate.ts:703-704 comment ADMITS it — verifier-side limit/respawn re-drive has resume=undefined→skipImplement=false→implementor branch→LoopCompositionError; verifier failover+auto-respawn BROKEN); F4 (schema.ts:94/98 harness/effort = z.string().min(1), NOT enum-validated → claude-code/xhigh parse; switch_model not pinned to role's effective harness). F2 (separate writes in driveFailoverOnLimit) + F8 (status wait/ETA) presumed live. → NOT committing failover with verifier-side broken; fix the cluster on the unstaged failover + commit together. Fix wave (review7-cluster): F1 re-entry (use resolveResumeEntry on the re-drive — it's at orchestrate.ts:215, returns first:'verify' for a completed-implementor/verifying round — affects auto-respawn AND failover) + F2 atomic failover-advance (position+desired-model+intent one crash-safe unit, retry reads the SAME rung) + F4 enum-validate harness(claude|codex)/effort + switch_model-pinned-to-effective-harness at dispatch + F8 status reads actual policy + structured resumesAt. THEN focused F3 wave (non-limit active-round strand → durable interrupted outcome + resume reclaims) + operational wave (F5 pin workspace/base at creation, F6 durable cadence + wire pre_model_switch/pre_verify_handoff, F7 wire worktree/artifact cleanup+gcSweep+SQL-GC, F9 runId-on-start-failure + list cmd + bin name harness-orchestrator→harness).

## 5hh. Review-7 cluster CONVERGED (wf_2c24b54c, 1383/80 green) — failover + F1/F2/F4/F8 committed together
Four fixes landed on the unstaged failover; both adversarial verifiers HOLD (11 sub-claims, each with a mutation proof that the test fails without the fix); mechanical green 1383 tests/80 files, tsc clean, index empty, npm pack OK.
- **F1 role re-entry (orchestrate.ts):** new `resolveRedriveReentry(service,runId)` reads the durable RoleRoundProjection + phase through the existing `resolveResumeEntry` (:215) and returns an override ONLY for a verifier round (first:'verify') — an implementor re-drive still takes the default implementor branch byte-for-byte. Both catch arms use it: the LimitPausedError arm DROPPED the `if(role==='verifier') throw` degrade and now routes verifier limits through driveFailoverOnLimit; the AutoRespawnSignal arm records it after recordSuccessorIntent. So verifier-side limit AND verifier-side crash both re-enter at VERIFICATION, not the implementor-branch LoopCompositionError. Fixes the same bug in the already-committed bfd859e auto-respawn. Mutation: neuter → the 4 verifier tests fail with the exact reviewed LoopCompositionError.
- **F2 failover-advance atomicity (service.ts driveFailoverOnLimit):** position read + desired-model pin + position advance + failover alert + recordSuccessorIntent wrapped in ONE transactionImmediate (nested store/DesiredModelStore/ingest/recordSuccessorIntent JOIN the outer IMMEDIATE txn per driver.ts); position read moved inside the lock (select→advance one serialized atom); spawn stays the post-commit side-effect. A crash mid-advance rolls position back WITH the intent → retry re-reads the SAME rung. Mutation: unwrap → position advances to rung 1 while intent throws (skips rung 0).
- **F4 config validation (schema.ts + service.ts):** (a) failoverLadderEntry harness = z.enum(HARNESSES), effort = z.enum(REASONING_EFFORTS) — imports the single runtime vocab from model-resolution.ts, no parallel list, no import cycle; claude-code/xhigh now REJECTED at parse. (b) dispatch guard `crossesHarnessUnderSwitchModel = policy==='switch_model' && targetHarness!==from.harness` folds into the T25 no-live-target degrade — a switch_model rung naming a foreign harness degrades-to-wait, never silently relabeled cross-harness. switch_harness cross-harness still allowed.
- **F8 status honesty (commands.ts):** buildLimitStatus reports policy = pinned.failoverPolicy (not literal 'wait'); top-level + human-readable ETA surface the structured incident.resumesAt when present; genuine-unknown stays 'unknown'.
Two non-blocking verifier observations noted (a defensive-only false-refuse edge in the `from` fallback that can't trigger in advertised scenarios; failover policy surfaced under limit.policy in status --json). §6.3 conformance UNCHANGED (no transitions added). PLAN.md/README mirrored. Committing failover + this cluster as ONE increment (the 6th unpushed commit). UI-DESIGN-BRIEF.md (untracked, non-sensitive design handoff) committed SEPARATELY as a docs commit. REMAINING review-7: F3 (deep non-limit active-round strand → durable interrupted outcome + resume reclaims) as its own focused wave; operational F5 (pin workspace/base at creation), F6 (durable cadence + wire pre_model_switch/pre_verify_handoff), F7 (wire worktree/artifact cleanup + gcSweep + SQL-GC), F9 (runId-on-start-failure + list cmd + bin name).

## 5ii. Post-cluster sequencing — F3 wave launched; operational = F5/F6/F7/F9; F7 = explicit confirm-before-delete cleanup (user decision 2026-07-20)
After committing failover+cluster (de7fa0a) + the design brief (5a5cf81), 7 commits ahead of origin, tree clean. Remaining review-7 work sequenced (all touch shared files service.ts/orchestrate.ts/commands.ts → SERIAL, never concurrent on the un-isolated tree):
- **F3 wave LAUNCHED (wf_0f88f4ee-a1c).** Orchestrator-confirmed LIVE on the committed tree by tracing runRole: round marked `active` + phase advanced at child.spawned (~2404), then `runner.run()` (~2443) runs the flow; the only inner catch wraps the spawn/pin window (~2379 → #routeProviderFailure); a non-limit/non-crash typed error thrown by runner.run() is UNROUTED → falls to the finally (#confirmChildStopped terminates the child) → propagates out (orchestrate.ts:755 re-throws non-Limit/non-Respawn). Leaves round `active`, phase implementing/verifying, suspension `none` → resume refuses ("not paused"), run refuses (not approved) → STRANDED (startup-reap only reconciles DEAD-owner records; here the owner unwound alive). Fix = route runner.run() flow errors through the same classification → durable `interrupted` (resumable via existing resolveResumeEntry/T13 path); genuinely-terminal (LoopComposition/ownership) stays terminal; report BLOCKED on an unresolved recoverable/terminal fork. Build xhigh + adversarial verify + mechanical.
- **Operational wave DRAFTED, held for post-F3 (scratchpad/operational-wave.js): F5 → F6 → F7 → F9 (serial).** F5 pin absolute workspace at creation + base commit at APPROVAL (recon-confirmed live: commands.ts:384/399/411 pass cmd.workspace verbatim, no path.resolve/base-pin). F6 durable cadence (recon: CadenceTracker now exists but held in a process-local `new Map()` at service.ts:1052 → still resets across processes, same class as the S5 breaker-window residual; reuse that durable-counter pattern) + wire pre_model_switch/pre_verify_handoff (unwired). F9 runId-on-start-failure + `list` command (recon: none exists) + bin name harness-orchestrator→harness (recon: package.json:8 = harness-orchestrator, docs say harness).
- **F7 = explicit confirm-before-delete cleanup FEATURE (USER DECISION 2026-07-20: "can add a feature and ask if worktrees can be cleaned up").** Resolves the retention tension WITHOUT changing the default: leave-worktree-for-review (orchestrate.ts:768) STAYS the default; add a dedicated operator-initiated cleanup that ASKS before removing — aligns with the design brief §15.5 (Delete/retention) + §20 (Missing artifact). Recon-confirmed live: removeWorktree (manager.ts) + gcSweep (store.ts:209) + SQL-metadata GC have ONLY test callers → nothing bounds accumulation. Design: a `clean`/`delete` CLI command that (a) REFUSES active/paused runs (terminal-only: merge_ready/failed/cancelled), (b) shows the worktree + artifact + event impact before acting (dry-run/preview), (c) CONFIRMS before removing (interactive y/N on a TTY; `--yes` to proceed non-interactively for scripts), (d) removes the worktree (removeWorktree) + artifact blobs + GCs the SQL metadata that REFERENCES them (gcSweep) while PRESERVING the immutable event-log/run record (per §15.5 "storage cleanup without pretending history still has evidence" — the run stays in `list`, artifacts marked absent). NEVER auto-deletes on terminal. If the "which SQL rows are safe to GC vs which are the audit trail" boundary is genuinely ambiguous, report BLOCKED naming it.

## 5jj. F3 DONE + regression-proven (1397/81 green, tsc clean) — active-round strand → resumable interrupt, NO new transition
Fix is ONE catch around `runner.run()` in `runRole` routing to a new `#interruptActiveRoundOnFlowError(ctx, error)`. Diagnosis CONFIRMED on the committed tree before editing (round `active` + phase advanced at child.spawned ~2408, `runner.run` ~2461, the only inner catch wraps the spawn/pin window ~2397→#routeProviderFailure). Design (all reuse, no new domain concept):
- **PRODUCER = T17, not a new trigger.** A recoverable typed flow error records `recovery.running_segment_found` (T17) — the SAME breaker-exempt interrupt the reap producer emits — so `resume` reclaims via the EXISTING T12/interrupted → resolveResumeEntry machinery a T13 crash-interrupt already uses. §6.3 UNCHANGED (no transition added → conformance NOT regenerated). CONFIRMED T17's precondition set (suspension none/phase non-terminal/child_active/generation_matches_active) is satisfiable at the catch point (child.spawned made the gen active; the finally's #confirmChildStopped is a no-op after T17 marks it stopped, exactly like T13/pause).
- **Recoverable/terminal split = by TYPE (the crux), positive-recoverable / default-terminal.** `isRecoverableRoundFlowError` = `isAdapterError` (auth/protocol arrive sink-safe from #routeProviderFailure) ∨ `BudgetExceededError` ∨ `isWorktreeError` (every local git subprocess throws one). Everything else — LoopCompositionError/RunOwnershipConflictError/other composition-ownership-invariant breaches, AND any untyped `Error` — stays TERMINAL (no interrupt), because a falsely-resumable terminal error would re-enter+re-throw forever (the task's explicit worse-failure), whereas a conservatively-terminal error is no worse than today's strand. Chose positive-recoverable to AVOID the service→orchestrate import cycle (can't import LoopCompositionError into service.ts) and to make the default safe.
- **Limit/crash BYTE-FOR-BYTE.** The catch re-throws LimitPausedError/AutoRespawnSignal untouched (the loop consumes them); a `suspension.kind !== 'none'` guard makes the autoRespawn=off T13 path (already interrupted before its sink-safe error reaches the catch) a clean no-op — proven by the "one T13, ZERO recovery.running_segment_found" test. Scoped to dispatched implementor/verifier rounds (a coordinator strand is already reclaimed by the W3-4 unsuspended re-drive; bare runRole has no round).
- **DOCUMENTED BOUNDARY (not blocked):** artifact IO throws plain `Error` (store.ts) with no discriminable type → treated as terminal-by-default (same as an invariant breach). No regression (plain-Error artifact failures strand today too); the primary F3 targets (auth/protocol/budget/git) are all cleanly typed. NOT a genuine fork → not reported BLOCKED; the safe direction (unknown ⇒ terminal) is what the task prioritizes.
- **Tests (`src/app/f3-active-round-strand.test.ts`, BOTH drivers, 14 tests):** auth on an active IMPLEMENTOR → interrupted (breaker-exempt) + resume reclaims the SAME round → merge_ready; auth on an active VERIFIER → interrupted + resume re-enters at VERIFICATION (F1 role-aware, implementor NOT re-dispatched) → merge_ready; protocol/budget/git each interrupt breaker-exempt; LoopCompositionError stays terminal (suspension none, resume refuses); crash-off unchanged. REGRESSION PROVEN: neuter the interrupt call → exactly the 5 recoverable cases ×2 drivers = 10 fail with the strand (`expected 'none' to be 'interrupted'`), terminal+crash-off still pass. PLAN.md (T17 row + Pause routing) + README (resume note) mirrored. Tree UNSTAGED for user review. REMAINING review-7: operational F5/F6/F9; F7 deferred (retention decision).

## 6. Standing uncertainties (post-Rev 3)
1. Live limit-error corpus per harness/version (gates P4a parsed tier). SOFTENED 2026-07-18 during P2: codex-acp@1.1.4 dist carries a structured discriminator (`data.codexErrorInfo==='usageLimitExceeded'`, no reset field) — source-verified by the adapter agent AND re-verified by orchestrator grep. Both MVP harnesses now have structured limit *kind* detection; the corpus gates only the optional parsed tier and ETA enrichment. 2. #886 magnitude → answered by the P2 gate. 3. Codex subscription headless usability (3-state reporting until known). 4. Per-role context budget values (measured in P4c). 5. ACP required/optional boundary drift (capability probe + pinned adapter versions hedge).
- PTY tier (dominated), A2A (wrong layer, zero adoption in harnesses), vector store (no retrieval need at MVP scale), AG-UI adoption now (single-run abstraction gap), auto-merge (user reviews unstaged consolidated tree per their workflow), parallel waves (schema reserves group id; execution deferred), Electron shell (core must not depend on it; design tokens wait for the UI phase).
